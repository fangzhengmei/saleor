# 折扣规则与优惠券计算逻辑报告

## 一、折扣类型体系

### 1.1 折扣类型 (DiscountType)

| 类型 | 说明 | 应用层级 |
|------|------|----------|
| `PROMOTION` | 目录级促销折扣 | 商品行级别 |
| `ORDER_PROMOTION` | 订单级促销折扣 | 订单级别 |
| `VOUCHER` | 优惠券折扣 | 商品行/订单/运费级别 |
| `MANUAL` | 手动折扣 | 商品行/订单级别 |
| `SALE` | 销售折扣（已废弃，迁移至 Promotion） | - |

### 1.2 优惠券类型 (VoucherType)

| 类型 | 说明 | 应用对象 |
|------|------|----------|
| `ENTIRE_ORDER` | 整单券 | 订单小计金额 |
| `SHIPPING` | 运费券 | 订单运费金额 |
| `SPECIFIC_PRODUCT` | 指定商品券 | 指定商品/分类/集合 |

### 1.3 促销类型 (PromotionType)

| 类型 | 说明 | 触发时机 |
|------|------|----------|
| `CATALOGUE` | 目录级促销 | 商品加入购物车时自动应用 |
| `ORDER` | 订单级促销 | 订单结算时根据订单条件应用 |

### 1.4 奖励类型 (RewardType)

| 类型 | 说明 |
|------|------|
| `SUBTOTAL_DISCOUNT` | 订单小计折扣 |
| `GIFT` | 赠品奖励 |

---

## 二、计算优先级与叠加策略

### 2.1 核心优先级顺序

**从高到低的优先级排序：**

```
手动折扣 (MANUAL)
    ↓
优惠券 (VOUCHER)
    ↓
订单级促销 (ORDER_PROMOTION)
    ↓
目录级促销 (PROMOTION)
    ↓
基础价格 (Base Price)
```

### 2.2 互斥规则

#### 2.2.1 优惠券与订单级促销互斥

- **规则**：当订单/购物车中已使用优惠券时，**不应用**订单级促销
- **代码依据**：`saleor/discount/utils/checkout.py:227-229`
  ```python
  # Discount from order rules is applied only when the voucher is not set
  if checkout.voucher_code:
      _clear_checkout_discount(checkout_info, lines_info, save)
      return None
  ```

#### 2.2.2 手动折扣与其他折扣互斥

- **规则**：手动折扣存在时，移除所有其他行级折扣
- **代码依据**：`saleor/discount/utils/checkout.py:143-150`
  ```python
  # manual line discount do not stack with other line discounts
  if [discount for discount in line_info.discounts if discount.type == DiscountType.MANUAL]:
      line_discounts_to_remove.extend(discounts_to_update)
      continue
  ```

#### 2.2.3 同一行只能有一个同类型折扣

- 通过 `unique_type` 字段和数据库唯一约束保证
- **代码依据**：`saleor/discount/models.py:567-572`
  ```python
  constraints = [
      models.UniqueConstraint(
          fields=["line_id", "unique_type"],
          name="unique_orderline_discount_type",
      ),
  ]
  ```

### 2.3 可叠加场景

#### 2.3.1 目录级促销 + 优惠券

- 目录级促销先应用于商品价格
- 优惠券可在此基础上继续应用
- **代码依据**：`saleor/checkout/base_calculations.py:38-65`
  ```python
  # The price includes catalogue promotions, specific product and applied once per order
  # voucher discounts. The price does not include order promotions and the entire order vouchers.
  ```

#### 2.3.2 多个目录级促销选最优

- 同一商品可匹配多个目录级促销规则
- 系统自动选择**折扣金额最大**的规则应用
- **代码依据**：`saleor/discount/interface.py:94-97`
  ```python
  listing_promotion_rule = max(
      list(listings_rules),
      key=lambda x: x.discount_amount,
  )
  ```

#### 2.3.3 订单级促销奖励类型

- 可选择**最优**奖励：折扣金额 vs 赠品价值
- **代码依据**：`saleor/discount/utils/promotion.py:311-313`
  ```python
  best_rule, best_discount_amount, gift_listing = max(
      rule_discounts, key=lambda x: x.discount_amount
  )
  ```

---

## 三、适用范围匹配逻辑

### 3.1 目录级促销 (Catalogue Promotion)

#### 3.1.1 匹配方式
- 通过 `catalogue_predicate` 字段定义匹配条件
- 支持按商品、分类、集合等维度筛选
- 匹配结果预计算并存储在 `VariantChannelListing.discounted_price_amount`

#### 3.1.2 关联关系
- `PromotionRule` 与 `ProductVariant` 多对多关联
- 后台任务批量更新商品与规则的关联关系
- **代码依据**：`saleor/discount/utils/promotion.py:518-563`

### 3.2 优惠券 (Voucher)

#### 3.2.1 适用范围字段

| 字段 | 说明 |
|------|------|
| `products` | 指定商品 |
| `variants` | 指定商品变体 |
| `collections` | 指定商品集合 |
| `categories` | 指定商品分类 |
| `countries` | 指定适用国家 |
| `channel_listings` | 指定适用渠道 |

#### 3.2.2 匹配逻辑
- **代码依据**：`saleor/discount/utils/voucher.py:220-253`
  - 若指定了任何范围字段，仅匹配范围内的商品
  - 若未指定任何范围字段，**所有商品**都适用
  - 赠品行 (`is_gift=True`) 不参与优惠券折扣

#### 3.2.3 应用门槛验证
- **最低消费** (`min_spent`)：订单小计需达到指定金额
- **最低商品数量** (`min_checkout_items_quantity`)：订单商品总数需达到指定数量
- **代码依据**：`saleor/discount/utils/voucher.py:327-340`

### 3.3 订单级促销 (Order Promotion)

#### 3.3.1 匹配方式
- 通过 `order_predicate` 字段定义订单条件
- 支持按订单金额、商品数量、客户信息等维度筛选
- **代码依据**：`saleor/discount/utils/promotion.py:566-608`

#### 3.3.2 触发时机
- 订单价格重新计算时触发
- 基于**基础价格（仅含目录级促销）**判断资格
- **代码依据**：`saleor/discount/utils/checkout.py:264-281`

---

## 四、与价格层级的协作

### 4.1 价格字段层级

```
undiscounted_base_unit_price (原始基础单价)
    ↓ 应用目录级促销
discounted_price_amount (目录促销价，预计算)
    ↓ 应用优惠券/手动折扣
base_unit_price_amount (最终基础单价)
    ↓ 应用税费/运费
total (订单总价)
```

### 4.2 各层级说明

| 字段 | 说明 | 存储位置 |
|------|------|----------|
| `undiscounted_base_unit_price` | 未折扣的原始单价 | OrderLine / CheckoutLine |
| `price_amount` | 商品挂牌价 | ProductVariantChannelListing |
| `discounted_price_amount` | 目录级促销后的价格（预计算） | ProductVariantChannelListing |
| `base_unit_price_amount` | 应用所有行级折扣后的基础单价 | OrderLine / CheckoutLine |
| `base_subtotal` | 仅含目录级促销的订单小计 | Order / Checkout |

### 4.3 价格更新流程

#### 4.3.1 目录级促销更新
- 促销规则变更时，标记规则为 `variants_dirty=True`
- 后台任务批量重新计算关联商品的 `discounted_price_amount`
- **代码依据**：`saleor/discount/utils/promotion.py:925-951`

#### 4.3.2 订单/购物车计算流程
1. 计算基础价格（含目录级促销）
2. 应用优惠券到符合条件的行
3. 应用订单级促销（无优惠券时）
4. 应用手动折扣
5. 计算最终价格

### 4.4 价格覆盖 (Price Override)

- 支持手动指定商品单价（`price_override`）
- 目录级促销在覆盖价格基础上重新计算
- **代码依据**：`saleor/discount/utils/promotion.py:202-219`
  ```python
  if price_override is not None:
      # calculate discount amount on overridden price
      discount = rule_info.rule.get_discount(channel.currency_code)
      discounted_price = discount(price_override)
      discount_amount = (price_override - discounted_price).amount
  ```

---

## 五、多币种处理细节

### 5.1 币种来源

- **订单/购物车币种**：由所属渠道 (`Channel.currency_code`) 决定
- 所有折扣金额都与订单币种保持一致

### 5.2 优惠券多币种配置

#### 5.2.1 渠道级配置
- 优惠券通过 `VoucherChannelListing` 与渠道关联
- 每个渠道可独立配置：
  - `discount_value`：折扣值（固定金额或百分比）
  - `min_spent`：最低消费门槛
  - `currency`：货币单位（与渠道币种一致）
- **代码依据**：`saleor/discount/models.py:144-169`

#### 5.2.2 折扣计算
- 固定金额折扣：直接使用渠道配置的金额
- 百分比折扣：基于订单金额按比例计算
- **代码依据**：`saleor/discount/models.py:171-176`

### 5.3 促销多币种配置

#### 5.3.1 渠道关联
- `PromotionRule` 与 `Channel` 多对多关联
- 规则仅在关联的渠道中生效

#### 5.3.2 折扣值配置
- 折扣值为纯数值，含义由 `reward_value_type` 决定
  - `FIXED`：按货币单位解释（与渠道币种一致）
  - `PERCENTAGE`：按百分比计算（与币种无关）

### 5.4 金额计算约束

- 所有金额计算后使用 `quantize_price` 量化到币种小数位
- 折扣金额不超过原始价格（最低为 0）
- **代码依据**：`saleor/discount/utils/voucher.py:645`
  ```python
  discount_amount = min(voucher_discount_amount, total_price)
  ```

---

## 六、计算流程总览

### 6.1 Checkout 计算流程

```
1. create_or_update_discount_objects_from_promotion_for_checkout()
   ├─ create_checkout_line_discount_objects_for_catalogue_promotions()
   │  └─ 为每行应用最优目录级促销
   └─ create_checkout_discount_objects_for_order_promotions()
      ├─ 检查是否有优惠券 → 有则清除订单促销
      ├─ 计算 base_subtotal（仅含目录促销）
      ├─ 匹配符合条件的订单级促销
      └─ 应用最优订单级促销或赠品
```

### 6.2 Order 计算流程

```
1. refresh_order_line_discount_objects_for_catalogue_promotions()
   └─ 更新行级目录促销折扣

2. create_or_update_voucher_discount_objects_for_order()
   ├─ create_or_update_discount_object_from_order_level_voucher()
   │  └─ 处理整单/运费优惠券
   └─ create_or_update_line_discount_objects_from_voucher()
      └─ 处理行级优惠券

3. handle_order_promotion()
   └─ 处理订单级促销（无优惠券时）
```

---

## 七、关键互斥关系总结表

| 已存在 | 可叠加 | 不可叠加 |
|--------|--------|----------|
| 目录级促销 | 优惠券、手动折扣 | - |
| 优惠券 | 目录级促销 | 订单级促销、手动折扣 |
| 订单级促销 | 目录级促销 | 优惠券、手动折扣 |
| 手动折扣 | 无 | 所有其他折扣 |
| 整单券 | 目录级促销 | 订单级促销、行级券、手动折扣 |
| 运费券 | 目录级促销、其他类型优惠券（除同类型） | 订单级促销、手动折扣 |
| 行级券 | 目录级促销、其他类型优惠券（除同类型） | 订单级促销、手动折扣 |

---

## 八、数据模型关系

```
Promotion (1) → (N) PromotionRule (N) → (N) ProductVariant
                              ↓ (N)
                        Channel (M)

Voucher (1) → (N) VoucherChannelListing (N) → (1) Channel
   ↓ (N)
VoucherCode (1) → (N) VoucherCustomer
```
