# 订单重算主路径深度说明

## 1. 订单重算主路径总览

### 1.1 核心入口函数

订单重算的统一入口是 `fetch_order_prices_if_expired`，整个计算链路遵循严格的执行顺序：

```
fetch_order_prices_if_expired
         ↓
  should_refresh_prices  ──→ 否 ──→ 直接返回
         ↓ 是
  prepare_order_lines_for_refresh
         ↓
  process_order_promotion (订单促销判定)
         ↓
  process_order_prices
         ├─→ calculate_prices (折扣计算)
         │    ├─→ base_order_subtotal
         │    └─→ propagate_order_discount_on_order_prices
         └─→ calculate_taxes (税费计算)
              ├─→ FLAT_RATES / TAX_APP
              └─→ 持久化更新
```

### 1.2 重算触发条件

```python
# should_refresh_prices 判定逻辑
if order.status not in ORDER_EDITABLE_STATUS:
    return False
if not force_update and not order.should_refresh_prices and not expired_line_ids:
    return False
if tax_strategy == TAX_APP and not allow_sync_webhooks:
    return False
return True
```

---

## 2. 并发命中时的判定与清除/保留步骤

### 2.1 初始状态准备（T0）

**步骤**：`fetch_draft_order_lines_info` + `attach_voucher_info`

**输入**：
- 数据库中现有的订单行（OrderLine）
- 已关联的折扣记录（OrderLineDiscount、OrderDiscount）

**输出**：`EditableOrderLineInfo` 列表，包含：
- `line`：订单行对象
- `discounts`：该行已有的所有折扣
- `channel_listing`：变体渠道定价信息
- `rules_info`：适用的目录促销规则
- `voucher_denormalized_info`：非规范化优惠券信息

**关键操作**：
1. 预加载关联数据（`prefetch_related`）
2. 按渠道过滤变体定价
3. 加载适用的目录促销规则信息
4. 附加优惠券信息到符合条件的行

---

### 2.2 目录促销处理（T1）

**函数**：`refresh_order_line_discount_objects_for_catalogue_promotions`

#### 2.2.1 判定逻辑

```
对每行依次处理：
  ├─→ 检查是否有 MANUAL 类型行折扣
  │    ├─→ 有：移除所有目录折扣，跳过后续处理
  │    └─→ 无：继续
  ├─→ 检查 is_discounted_line_by_catalogue_promotion
  │    ├─→ 否：移除所有目录折扣，跳过
  │    └─→ 是：继续
  ├─→ 检查 line.is_gift
  │    ├─→ 是：移除所有目录折扣，跳过
  │    └─→ 否：继续
  └─→ 检查 rules_info（适用的促销规则）
       ├─→ 空：移除所有目录折扣
       └─→ 非空：取第一条规则（最优）
            ├─→ 无现有折扣：创建新折扣
            └─→ 有现有折扣：更新折扣信息
```

#### 2.2.2 清除/保留规则

| 条件 | 处理方式 |
|------|---------|
| 存在 MANUAL 行折扣 | 清除所有目录折扣，保留 MANUAL |
| 行是礼品（is_gift=True） | 清除所有目录折扣 |
| 无适用促销规则 | 清除所有目录折扣 |
| 有适用规则且无现有折扣 | 创建新折扣 |
| 有适用规则且有现有折扣 | 更新现有折扣 |
| 多个目录折扣（历史数据） | 保留第一个，清除其余 |

#### 2.2.3 基础单价更新

```python
# _update_base_unit_price_amount_for_catalogue_promotion
base_unit_price = undiscounted_base_unit_price_amount
for discount in catalogue_discounts:
    unit_discount = discount.amount_value / quantity
    base_unit_price -= unit_discount
base_unit_price_amount = max(base_unit_price, 0)
```

---

### 2.3 行级手动折扣处理（T2）

**函数**：`refresh_manual_line_discount_object`

#### 2.3.1 判定逻辑

```
对每行依次处理：
  ├─→ 检查是否有 MANUAL 类型行折扣
  │    └─→ 无：跳过
  ├─→ 基于 undiscounted_base_unit_price 重新计算折扣后价格
  ├─→ 更新 line.base_unit_price_amount
  └─→ 如有必要，更新折扣的 amount_value
```

#### 2.3.2 关键特性

- 手动行折扣**不与其他行级折扣叠加**
- 计算基准是 `undiscounted_base_unit_price`（原价）
- 折扣后价格不能低于 0

---

### 2.4 订单促销判定（T3）

**函数**：`handle_order_promotion` → `create_order_discount_objects_for_order_promotions`

#### 2.4.1 前置互斥检查

```python
# 关键互斥逻辑
if order.voucher_code or order.discounts.filter(type=DiscountType.MANUAL):
    _clear_order_discount(order, lines_info)
    return
```

| 已存在折扣 | 处理方式 |
|-----------|---------|
| 有优惠券（voucher_code 非空） | 清除所有 ORDER_PROMOTION 折扣，返回 |
| 有手动订单折扣 | 清除所有 ORDER_PROMOTION 折扣，返回 |
| 无上述折扣 | 继续订单促销判定 |

#### 2.4.2 订单促销匹配流程

```
1. 获取所有活动的订单促销规则（order_predicate 非空）
2. 过滤：规则必须适用于当前渠道
3. 对每条规则：
   ├─→ 用 order_predicate 匹配当前订单
   │    └─→ filter_qs_by_predicate 执行匹配
   └─→ 匹配成功：加入 applicable_rules 列表
4. 对所有适用规则：
   ├─→ 计算每条规则的折扣金额
   ├─→ 礼品规则以礼品价格作为折扣金额
   └─→ 选择折扣金额最大的规则（最优规则）
5. 应用最优规则：
   ├─→ 礼品规则：创建礼品行 + 行折扣
   └─→ 折扣规则：创建订单级折扣
```

#### 2.4.3 清除操作（`_clear_order_discount`）

```python
def _clear_order_discount(order, lines_info):
    delete_gift_line(order, lines_info)  # 删除礼品行
    order.discounts.filter(type=DiscountType.ORDER_PROMOTION).delete()
```

---

### 2.5 订单级折扣应用（T4）

**函数**：`propagate_order_discount_on_order_prices`

#### 2.5.1 折扣遍历顺序

```
对 order.discounts.all() 按数据库顺序依次处理：
  ├─→ VOUCHER + ENTIRE_ORDER：应用到 subtotal
  ├─→ VOUCHER + SHIPPING：跳过（已在 base_shipping_price 中）
  ├─→ ORDER_PROMOTION：应用到 subtotal
  └─→ MANUAL：
       ├─→ PERCENTAGE：同时应用到 subtotal 和 shipping_price
       └─→ FIXED：在 subtotal 和 shipping_price 间按比例分摊
```

#### 2.5.2 手动固定金额分摊逻辑

```
前提：temporary_undiscounted_total = subtotal + shipping_price

if temporary_undiscounted_total > 0:
    temporary_total = apply_discount(temporary_undiscounted_total)
    total_discount = temporary_undiscounted_total - temporary_total
    
    subtotal_discount = (subtotal / temporary_undiscounted_total) × total_discount
    shipping_discount = total_discount - subtotal_discount
    
    subtotal -= subtotal_discount
    shipping_price -= shipping_discount
```

---

### 2.6 订单级折扣分摊到行（T5）

**函数**：`apply_subtotal_discount_to_order_lines`

#### 2.6.1 分摊规则

| 订单行数 | 分摊方式 |
|---------|---------|
| 1 行 | 全部折扣应用到该行 |
| N 行（N>1） | 按各行金额比例分摊，最后一行吸收余数 |

#### 2.6.2 多行分摊算法

```
已知：subtotal_discount（订单级折扣总额）
      lines（订单行列表，共 N 行）
      base_subtotal（折扣前小计）

for i from 0 to N-1:
    if i < N-1:
        share = (line_i.base_unit_price × quantity_i) / base_subtotal
        discount_i = quantize(share × subtotal_discount)
        remaining_discount -= discount_i
    else:  # 最后一行
        discount_i = remaining_discount
    
    line_i.total_price = max(line_i.base_total - discount_i, 0)
```

#### 2.6.3 单价反推

```python
# assign_order_line_prices
line.total_price_net = total_price
if quantity > 0:
    line.unit_price_net = total_price / quantity
```

---

## 3. 固定金额折扣多币种对账级示例

### 3.1 场景设定

**基础数据**：
- 币种：美元（USD），精度 2 位小数
- 订单行：
  - Line A：单价 $99.99，数量 1
  - Line B：单价 $49.99，数量 1
  - Line C：单价 $29.99，数量 1
- 运费：$15.00
- 手动折扣：固定金额 $25.00（订单级）
- 税率：10%

### 3.2 计算过程详解

#### 3.2.1 阶段 1：行级折扣（目录促销）

假设 Line A 有 $10.00 目录促销折扣：

```
Line A:
  undiscounted_base_unit_price = $99.99
  catalogue_discount.amount = $10.00
  base_unit_price = $99.99 - $10.00 = $89.99
  base_total = $89.99 × 1 = $89.99

Line B:
  undiscounted_base_unit_price = $49.99
  base_unit_price = $49.99
  base_total = $49.99

Line C:
  undiscounted_base_unit_price = $29.99
  base_unit_price = $29.99
  base_total = $29.99

订单小计（base_subtotal） = $89.99 + $49.99 + $29.99 = $169.97
```

#### 3.2.2 阶段 2：订单级固定金额折扣分摊

**手动折扣**：$25.00（FIXED 类型，订单级）

```
temporary_undiscounted_total = $169.97 + $15.00 = $184.97

分摊比例：
  商品小计分摊比例 = $169.97 / $184.97 ≈ 0.9189057685
  运费分摊比例 = $15.00 / $184.97 ≈ 0.0810942315

折扣分摊：
  商品小计折扣 = $25.00 × 0.9189057685 ≈ $22.9726442124
  运费折扣 = $25.00 - $22.9726442124 = $2.0273557876

量化（保留 2 位小数）：
  商品小计折扣 = $22.97
  运费折扣 = $2.03

验证：$22.97 + $2.03 = $25.00 ✓

折扣后：
  subtotal = $169.97 - $22.97 = $147.00
  shipping_price = $15.00 - $2.03 = $12.97
```

#### 3.2.3 阶段 3：订单折扣分摊到订单行

**订单折扣总额**：$22.97

```
base_subtotal = $169.97

各行分摊比例：
  Line A: $89.99 / $169.97 ≈ 0.5294463728
  Line B: $49.99 / $169.97 ≈ 0.2941107254
  Line C: $29.99 / $169.97 ≈ 0.1764429016

折扣计算（前 N-1 行）：
  Line A 折扣 = quantize($22.97 × 0.5294463728) = quantize($12.1617791375) = $12.16
  Line B 折扣 = quantize($22.97 × 0.2941107254) = quantize($6.7557034076) = $6.76
  
  剩余折扣 = $22.97 - $12.16 - $6.76 = $4.05

最后一行（Line C）：
  Line C 折扣 = $4.05

验证分摊合计：$12.16 + $6.76 + $4.05 = $22.97 ✓
```

#### 3.2.4 阶段 4：各行价格更新

```
Line A:
  base_total = $89.99
  order_discount = $12.16
  total_price_net = $89.99 - $12.16 = $77.83
  unit_price_net = $77.83 / 1 = $77.83

Line B:
  base_total = $49.99
  order_discount = $6.76
  total_price_net = $49.99 - $6.76 = $43.23
  unit_price_net = $43.23 / 1 = $43.23

Line C:
  base_total = $29.99
  order_discount = $4.05
  total_price_net = $29.99 - $4.05 = $25.94
  unit_price_net = $25.94 / 1 = $25.94

验证订单小计：$77.83 + $43.23 + $25.94 = $147.00 ✓
```

#### 3.2.5 阶段 5：税费计算

```
税率：10%

Line A 税费 = $77.83 × 10% = $7.783 → 量化 $7.78
Line A 总价（含税）= $77.83 + $7.78 = $85.61

Line B 税费 = $43.23 × 10% = $4.323 → 量化 $4.32
Line B 总价（含税）= $43.23 + $4.32 = $47.55

Line C 税费 = $25.94 × 10% = $2.594 → 量化 $2.59
Line C 总价（含税）= $25.94 + $2.59 = $28.53

运费税费 = $12.97 × 10% = $1.297 → 量化 $1.30
运费总价（含税）= $12.97 + $1.30 = $14.27

订单总净价 = $147.00 + $12.97 = $159.97
订单总税费 = $7.78 + $4.32 + $2.59 + $1.30 = $16.00-
订单总价（含税）= $159.97 + $16.00 = $175.97

验证（按含税总价汇总）：
$85.61 + $47.55 + $28.53 + $14.27 = $175.96

⚠️ 尾差：$175.97 - $175.96 = $0.01
原因：各行税费分别量化后汇总，与订单总税费有 0.01 差异
处理方式：以订单级税费计算为准，行级税费仅作展示
```

#### 3.2.6 对账对照表

| 项目 | 金额 | 说明 |
|------|------|------|
| 商品原价合计 | $179.97 | $99.99 + $49.99 + $29.99 |
| 目录促销折扣 | -$10.00 | Line A 折扣 |
| 商品小计（折扣前） | $169.97 | |
| 手动订单折扣（商品部分） | -$22.97 | 分摊自 $25.00 |
| 商品小计（折扣后） | $147.00 | |
| 运费原价 | $15.00 | |
| 手动订单折扣（运费部分） | -$2.03 | 分摊自 $25.00 |
| 运费（折扣后） | $12.97 | |
| 订单总净价 | $159.97 | $147.00 + $12.97 |
| 税费（10%） | $16.00 | $159.97 × 10% |
| **订单总价（含税）** | **$175.97** | |

---

### 3.3 多币种示例：欧元（EUR）

**场景**：相同商品，欧元定价，精度 2 位小数

- Line A：€89.99，数量 1（目录促销 €10.00）
- Line B：€44.99，数量 1
- Line C：€26.99，数量 1
- 运费：€12.00
- 手动折扣：€20.00
- 税率：21%（欧盟标准 VAT）

```
阶段 1：目录促销后
Line A 基础总价 = €89.99 - €10.00 = €79.99
Line B 基础总价 = €44.99
Line C 基础总价 = €26.99
base_subtotal = €79.99 + €44.99 + €26.99 = €151.97

阶段 2：订单折扣分摊
temporary_undiscounted_total = €151.97 + €12.00 = €163.97
商品分摊比例 = €151.97 / €163.97 ≈ 0.9268158809
运费分摊比例 = €12.00 / €163.97 ≈ 0.0731841191

商品小计折扣 = quantize(€20.00 × 0.9268158809) = €18.54
运费折扣 = €20.00 - €18.54 = €1.46

subtotal = €151.97 - €18.54 = €133.43
shipping_price = €12.00 - €1.46 = €10.54

阶段 3：分摊到行
Line A: €18.54 × (€79.99/€151.97) = €9.765... → €9.77
Line B: €18.54 × (€44.99/€151.97) = €5.491... → €5.49
Line C: €18.54 - €9.77 - €5.49 = €3.28

Line A 总价 = €79.99 - €9.77 = €70.22
Line B 总价 = €44.99 - €5.49 = €39.50
Line C 总价 = €26.99 - €3.28 = €23.71

阶段 4：税费（21%）
订单总净价 = €133.43 + €10.54 = €143.97
税费 = €143.97 × 21% = €30.2337 → €30.23
订单总价 = €143.97 + €30.23 = €174.20
```

---

### 3.4 尾差处理原则

1. **分摊尾差**：最后一行吸收，确保分摊总和等于折扣总额
2. **税费尾差**：
   - 行级税费分别量化展示
   - 订单级税费按总净价重新计算
   - 以订单级税费为准进行财务核算
3. **舍入顺序**：
   - 折扣计算后量化
   - 分摊后量化
   - 税费计算后量化
   - 避免多次量化累积误差

---

## 4. 各阶段输入输出字段变化

### 4.1 订单行（OrderLine）字段

| 字段 | 阶段 | 输入来源 | 输出说明 |
|------|------|---------|---------|
| `undiscounted_base_unit_price_amount` | 初始 | 商品定价 | 保持不变（原价） |
| `base_unit_price_amount` | T1/T2 | 目录促销/手动折扣 | 原价 - 行级折扣单位金额 |
| `unit_price_net_amount` | T5 | 订单折扣分摊 | 基础单价 - 订单折扣分摊单位金额 |
| `unit_price_gross_amount` | 税费后 | 税费计算 | unit_price_net × (1 + 税率) |
| `total_price_net_amount` | T5 | 订单折扣分摊 | unit_price_net × 数量 |
| `total_price_gross_amount` | 税费后 | 税费计算 | total_price_net × (1 + 税率) |
| `undiscounted_unit_price_net_amount` | T5 | 原价 | = undiscounted_base_unit_price |
| `undiscounted_unit_price_gross_amount` | 税费后 | 原价 + 税费 | undiscounted_base_unit_price × (1 + 税率) |
| `undiscounted_total_price_net_amount` | T5 | 原价 × 数量 | undiscounted_base_unit_price × 数量 |
| `undiscounted_total_price_gross_amount` | 税费后 | 原价 + 税费 | undiscounted_total_price_net × (1 + 税率) |
| `tax_rate` | 税费后 | 税务配置/税务应用 | 税率（如 0.10 表示 10%） |
| `unit_discount_amount` | T1/T2 | 行级折扣 | 行级折扣单位金额合计 |
| `unit_discount_type` | T1/T2 | 折扣类型 | FIXED / PERCENTAGE |
| `unit_discount_value` | T1/T2 | 折扣配置 | 折扣值（如 10.00 或 0.10） |
| `unit_discount_reason` | T1/T2 | 折扣原因 | 折扣说明字符串 |

### 4.2 订单（Order）字段

| 字段 | 阶段 | 输入来源 | 输出说明 |
|------|------|---------|---------|
| `subtotal_net_amount` | T4 | 行级折扣后 | Σ(line.base_total) - 订单级折扣 |
| `subtotal_gross_amount` | 税费后 | 税费计算 | subtotal_net + 商品税费 |
| `total_net_amount` | T4 | 小计 + 运费 | subtotal_net + shipping_price_net |
| `total_gross_amount` | 税费后 | 税费计算 | total_net + 总税费 |
| `undiscounted_total_net_amount` | T4 | 原价合计 | Σ(line.undiscounted_total_price_net) + undiscounted_shipping |
| `undiscounted_total_gross_amount` | 税费后 | 原价 + 税费 | undiscounted_total_net × (1 + 税率) |
| `shipping_price_net_amount` | T4 | 运费折扣后 | base_shipping_price - 运费折扣 |
| `shipping_price_gross_amount` | 税费后 | 税费计算 | shipping_price_net × (1 + 税率) |
| `base_shipping_price_amount` | 初始 | 运费配置 | 运费原价（可能含运费优惠券折扣） |
| `undiscounted_base_shipping_price_amount` | 初始 | 运费配置 | 运费原价（无任何折扣） |
| `shipping_tax_rate` | 税费后 | 税务配置 | 运费税率 |
| `tax_error` | 税费后 | 税务计算 | 税务计算错误信息 |
| `should_refresh_prices` | T5 | 计算完成 | 设置为 False |

### 4.3 订单折扣（OrderDiscount）字段

| 字段 | 说明 |
|------|------|
| `type` | 折扣类型：VOUCHER / ORDER_PROMOTION / MANUAL |
| `value_type` | 折扣值类型：FIXED / PERCENTAGE |
| `value` | 折扣值（如 10.00 或 0.10） |
| `amount_value` | 实际折扣金额（量化后） |
| `currency` | 币种 |
| `name` | 折扣名称 |
| `reason` | 折扣原因 |
| `voucher_id` | 关联优惠券 ID（VOUCHER 类型） |
| `promotion_rule_id` | 关联促销规则 ID（ORDER_PROMOTION 类型） |

### 4.4 订单行折扣（OrderLineDiscount）字段

| 字段 | 说明 |
|------|------|
| `line_id` | 关联订单行 ID |
| `type` | 折扣类型：PROMOTION / VOUCHER / MANUAL |
| `value_type` | 折扣值类型：FIXED / PERCENTAGE |
| `value` | 折扣值 |
| `amount_value` | 实际折扣金额（整行） |
| `currency` | 币种 |
| `name` | 折扣名称 |
| `reason` | 折扣原因 |
| `voucher_id` | 关联优惠券 ID |
| `promotion_rule_id` | 关联促销规则 ID |
| `unique_type` | 唯一类型标识（用于互斥检查） |

---

## 5. 字段流转全景图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          字段流转全景图                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  商品定价                                                               │
│    ┌─ undiscounted_base_unit_price_amount (常量)                        │
│    │                                                                   │
│    ↓                                                                   │
│  T1 目录促销                                                           │
│    ├─ 计算 catalogue_discount.amount_value                             │
│    ├─ 更新 base_unit_price_amount                                      │
│    └─ 创建/更新 OrderLineDiscount (type=PROMOTION)                     │
│                                                                         │
│    ↓                                                                   │
│  T2 行级手动折扣（如存在）                                             │
│    ├─ 清除目录折扣（互斥）                                             │
│    ├─ 重新计算 base_unit_price_amount                                  │
│    └─ 更新 OrderLineDiscount (type=MANUAL)                             │
│                                                                         │
│    ↓                                                                   │
│  T3 订单促销判定                                                       │
│    ├─ 检查 voucher_code / manual 折扣（互斥）                          │
│    ├─ 匹配 order_predicate                                             │
│    ├─ 选择最优规则                                                     │
│    └─ 创建 OrderDiscount (type=ORDER_PROMOTION) 或礼品行              │
│                                                                         │
│    ↓                                                                   │
│  T4 订单级折扣应用                                                     │
│    ├─ 遍历 order.discounts                                             │
│    ├─ 计算 subtotal / shipping_price 折扣后金额                        │
│    ├─ 更新 OrderDiscount.amount_value                                  │
│    └─ 计算订单级折扣分摊到各行的金额                                   │
│                                                                         │
│    ↓                                                                   │
│  T5 订单折扣分摊到行                                                   │
│    ├─ 按比例分摊（最后一行吸收余数）                                   │
│    ├─ 更新 unit_price_net_amount                                       │
│    ├─ 更新 total_price_net_amount                                      │
│    └─ 更新 undiscounted_* 字段（保持原价参考）                         │
│                                                                         │
│    ↓                                                                   │
│  税费计算                                                              │
│    ├─ 调用税务插件/税务应用                                            │
│    ├─ 计算各行税费                                                     │
│    ├─ 计算运费税费                                                     │
│    ├─ 更新 unit_price_gross_amount                                     │
│    ├─ 更新 total_price_gross_amount                                    │
│    ├─ 更新 shipping_price_gross_amount                                 │
│    ├─ 更新 subtotal_gross_amount                                       │
│    └─ 更新 total_gross_amount                                          │
│                                                                         │
│    ↓                                                                   │
│  持久化                                                                │
│    ├─ 乐观锁检查（updated_at）                                         │
│    ├─ 保存 Order 字段                                                  │
│    └─ 批量更新 OrderLine 字段                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 关键设计决策说明

### 6.1 互斥优先原则

- 手动折扣优先级最高，与其他折扣互斥
- 订单促销与优惠券/手动折扣互斥
- 设计意图：减少计算复杂度，避免折扣滥用

### 6.2 最优折扣选择

- 目录促销：取折扣金额最大的规则
- 订单促销：取折扣金额最大的规则
- 礼品规则：以礼品价格作为折扣金额参与比较

### 6.3 尾差吸收策略

- 多行分摊时最后一行吸收余数
- 避免四舍五入累积误差
- 确保财务对账平衡

### 6.4 并发安全

- 行级锁（`select_for_update`）保护关键操作
- 乐观锁（`updated_at`）检测并发修改
- 幂等设计确保重复计算安全

---

## 7. 对账核对清单

### 7.1 折扣核对

- [ ] 目录折扣金额 = 原价 - 基础单价 × 数量
- [ ] 订单折扣总额 = 各订单行订单折扣分摊之和
- [ ] 手动固定折扣分摊 = 商品分摊 + 运费分摊
- [ ] 折扣后价格 ≥ 0

### 7.2 税费核对

- [ ] 行级税费 = 行净价 × 税率（量化后）
- [ ] 订单总税费 = 订单总净价 × 税率（量化后）
- [ ] 订单总价 = 订单总净价 + 订单总税费

### 7.3 字段一致性

- [ ] `subtotal_net_amount` = Σ(`line.total_price_net_amount`)
- [ ] `total_net_amount` = `subtotal_net_amount` + `shipping_price_net_amount`
- [ ] `undiscounted_total_net_amount` = Σ(`line.undiscounted_total_price_net_amount`) + `undiscounted_base_shipping_price_amount`
