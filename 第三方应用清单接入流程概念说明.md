# Saleor 第三方应用清单接入流程概念说明

## 概述

第三方应用通过清单文件（Manifest）接入 Saleor 是一个涉及多模块协作的完整流程，包括清单获取、格式校验、权限评估、应用注册、Token 颁发和 Webhook 通道配置等核心环节。

## 一、清单文件（Manifest）结构

### 1.1 核心字段

清单文件是一个 JSON 格式的描述文件，定义了第三方应用的完整元数据：

| 字段 | 说明 | 必要性 |
|------|------|--------|
| `id` | 应用唯一标识符，全局唯一 | 必填 |
| `name` | 应用名称 | 必填 |
| `version` | 应用版本号 | 必填 |
| `permissions` | 应用请求的权限列表 | 可选 |
| `appUrl` | 应用主页 URL | 可选 |
| `tokenTargetUrl` | 接收安装 Token 的回调地址 | 可选 |
| `extensions` | UI 扩展点配置 | 可选 |
| `webhooks` | Webhook 订阅配置 | 可选 |
| `requiredSaleorVersion` | 要求的 Saleor 版本范围 | 可选 |

### 1.2 清单验证层级

清单文件的验证分为两个阶段：

**第一阶段：结构验证（Pydantic Schema）**
- 必填字段存在性检查
- 字段类型校验
- URL 格式合法性校验
- Brand Logo 图片格式与大小验证

**第二阶段：业务逻辑验证**
- 权限有效性校验（权限是否存在）
- 权限范围限制（禁止授予 `MANAGE_APPS` 权限）
- 应用标识符唯一性检查
- Saleor 版本兼容性检查
- 扩展点 URL 与 AppUrl 关联性校验
- Webhook 事件类型与订阅查询有效性

## 二、应用注册流程

### 2.1 完整流程图

```
用户触发安装
    ↓
[AppInstall Mutation]
    ├─ 校验 manifest_url 格式合法性
    ├─ 校验请求者是否拥有 MANAGE_APPS 权限
    ├─ 校验请求者是否拥有待授予的所有权限
    └─ 创建 AppInstallation 记录（PENDING 状态）
            ↓
[install_app_task 异步任务]
    ├─ 1. 拉取 Manifest 文件
    ├─ 2. 完整校验 Manifest 内容
    ├─ 3. 创建 App 记录（is_installed=False）
    ├─ 4. 关联权限到 App
    ├─ 5. 创建 AppExtension 记录（如需要）
    ├─ 6. 创建 Webhook 及 WebhookEvent 记录（如需要）
    ├─ 7. 生成 Token 并发送到 tokenTargetUrl
    ├─ 8. 标记 App.is_installed = True
    ├─ 9. 删除 AppInstallation 记录
    └─ 触发 APP_INSTALLED Webhook 事件
```

### 2.2 关键入口点

**GraphQL Mutation:**
- `appFetchManifest` - 仅获取并校验清单，不执行安装
- `appInstall` - 触发完整安装流程

**核心模块:**
- `saleor/app/manifest_validations.py` - 清单验证逻辑
- `saleor/app/installation_utils.py` - 安装执行逻辑
- `saleor/app/tasks.py` - 异步任务处理

## 三、权限授予机制

### 3.1 权限范围控制

**全局禁止权限:**
- `MANAGE_APPS` - 应用管理权限，禁止授予任何第三方应用，防止权限提升攻击

**请求者权限校验:**
- 执行安装的用户必须拥有 `MANAGE_APPS` 权限
- 用户不能授予自己不拥有的权限
- 超级用户不受权限范围限制

### 3.2 权限层级

应用权限分为两个层级：

1. **应用级权限** (`App.permissions`)
   - 定义应用整体可访问的资源范围
   - 在清单文件 `permissions` 字段中声明

2. **扩展点级权限** (`AppExtension.permissions`)
   - 单个 UI 扩展点所需的权限
   - 必须是应用级权限的子集
   - 用于在 UI 层面做细粒度控制

### 3.3 权限校验流程

```
Manifest.permissions
    ↓
[_ensure_app_permissions_allowed] 检查是否包含 MANAGE_APPS
    ↓
[_clean_permissions] 校验权限是否存在于系统中
    ↓
[ensure_can_manage_permissions] 校验请求者是否拥有这些权限
    ↓
关联到 AppInstallation.permissions
    ↓
安装时复制到 App.permissions
```

## 四、回调地址校验

### 4.1 URL 校验规则

使用自定义的 `AppURLValidator` 进行校验：

**允许的协议:**
- 标准 Webhook：`http`, `https`
- 消息队列：`awssqs`, `gcpubsub`
- 清单文件 URL：`http`, `https`

**校验逻辑:**
- 主机名格式合法性（支持 localhost、IPv4、IPv6）
- 端口号格式（可选，2-5 位数字）
- 资源路径格式
- 不允许的协议会直接拒绝

### 4.2 相对 URL 处理

扩展点 URL 支持相对路径（以 `/` 开头），但必须满足以下条件之一：
- `extension.target == "APP_PAGE"`（应用内页面跳转）
- 清单文件中提供了 `appUrl` 作为基础 URL

### 4.3 Token 回调地址

`tokenTargetUrl` 是应用接收授权 Token 的端点：
- 必须是有效的 HTTP/HTTPS URL
- Saleor 会向该地址发送 POST 请求，Body 为 `{"auth_token": "<token>"}`
- 请求头包含 `Saleor-Domain`、`Saleor-Api-Url`、`Saleor-Schema-Version`
- 如果 Token 发送失败，整个安装流程回滚

## 五、Token 颁发机制

### 5.1 Token 生成

**触发条件:**
- 清单文件中声明了 `tokenTargetUrl`

**Token 属性:**
- 使用 `oauthlib.common.generate_token()` 生成 32 字符随机字符串
- 数据库仅存储哈希值（使用 Django 的 `make_password`）
- 明文 Token 仅在创建时返回，之后无法查看
- 存储 `token_last_4` 用于用户识别

### 5.2 Token 发送流程

```
[AppToken.objects.create] 生成 Token 对（明文+哈希）
    ↓
明文 Token 临时保存在内存中
    ↓
[send_app_token] POST 到 tokenTargetUrl
    ├─ Headers: Saleor-Domain, Saleor-Api-Url, Saleor-Schema-Version
    └─ Body: {"auth_token": "<明文token>"}
    ↓
接收方返回 2xx 状态码？
    ├─ 是 → 继续安装流程
    └─ 否 → 删除已创建的 App，回滚整个安装
```

### 5.3 安全设计

- Token 仅通过 HTTPS 传输
- 发送失败立即回滚，避免残留无效 App 记录
- 数据库中不存储明文 Token
- 支持多 Token 机制，每个 App 可拥有多个独立 Token

## 六、对 Webhook 通道的影响

### 6.1 Webhook 自动创建

安装流程会根据清单中的 `webhooks` 字段自动创建：

```python
Webhook.objects.bulk_create([
    Webhook(
        app=app,
        name=webhook["name"],
        is_active=webhook["isActive"],
        target_url=webhook["targetUrl"],
        subscription_query=webhook["query"],
        custom_headers=webhook.get("customHeaders"),
    )
    for webhook in manifest_data.get("webhooks", [])
])
```

### 6.2 事件类型校验

**异步事件 (`asyncEvents`):**
- 必须是 `WebhookEventAsyncType` 中定义的合法事件
- 如：`ORDER_CREATED`, `PRODUCT_UPDATED` 等

**同步事件 (`syncEvents`):**
- 必须是 `WebhookEventSyncType` 中定义的合法事件
- 如：`PAYMENT_AUTHORIZE`, `SHIPPING_LIST_METHODS_FOR_CHECKOUT` 等

**订阅查询 (`subscription_query`):**
- 必须是有效的 GraphQL 订阅查询
- 查询中引用的事件类型自动生效

### 6.3 权限与 Webhook 联动

Webhook 的事件分发受应用权限控制：

```python
# AppQueryset.for_event_type 逻辑
def for_event_type(self, event_type: str):
    required_permission = WebhookEventAsyncType.PERMISSIONS.get(event_type, ...)
    if required_permission:
        # 过滤拥有所需权限的 App
        app_label, codename = required_permission.value.split(".")
        permissions = {
            "permissions__content_type__app_label": app_label,
            "permissions__codename": codename
        }
    return self.filter(
        is_active=True,
        webhooks__is_active=True,
        webhooks__events__event_type=event_type,
        **permissions,
    )
```

**关键规则:**
- 即使 Webhook 订阅了某个事件，如果应用没有对应的权限，事件也不会被分发
- 应用被禁用（`is_active=False`）时，所有相关 Webhook 停止接收事件
- Webhook 被禁用（`is_active=False`）时，单独停止该 Webhook

### 6.4 Webhook 生命周期

**创建:**
- 安装时根据清单批量创建
- 自动关联到所属 App
- 默认为激活状态（除非清单中指定 `isActive: false`）

**删除:**
- 应用删除时级联删除所有关联 Webhook
- 同时清理所有待发送的 EventDelivery、EventDeliveryAttempt、EventPayload
- 清理关联的私有存储文件

## 七、关键安全控制点

| 控制点 | 位置 | 目的 |
|--------|------|------|
| MANAGE_APPS 权限禁止 | `_ensure_app_permissions_allowed` | 防止应用获得应用管理权限 |
| 请求者权限范围校验 | `ensure_can_manage_permissions` | 防止越权授予权限 |
| Token 发送失败回滚 | `install_app` | 避免残留无效的 App 记录 |
| App 标识符唯一性 | `clean_manifest_data` | 防止重复安装 |
| Webhook 权限过滤 | `AppQueryset.for_event_type` | 确保事件只发送给有权限的应用 |
| URL 协议白名单 | `AppURLValidator` | 防止 SSRF 攻击 |

## 八、错误处理机制

### 8.1 安装失败场景

1. **Manifest 获取失败** - 网络超时、HTTP 错误、JSON 解析错误
2. **Manifest 验证失败** - 字段缺失、格式错误、权限无效
3. **Token 发送失败** - 回调地址不可用、返回非 2xx 状态码
4. **数据库操作失败** - 唯一键冲突、事务异常

### 8.2 失败处理

- `AppInstallation` 状态标记为 `FAILED`
- 错误消息记录到 `message` 字段
- 已创建的资源（App、Webhook 等）全部回滚删除
- 失败的安装记录保留，供用户排查

## 九、模块协作关系

```
GraphQL Layer (saleor/graphql/app)
    │
    ├─ mutations/app_install.py       # Mutation 入口
    ├─ mutations/app_fetch_manifest.py # 清单预览
    └─ utils.py                        # 权限校验工具
        │
        ▼
App Domain (saleor/app)
    ├─ models.py                       # App, AppToken, AppInstallation 模型
    ├─ manifest_schema.py              # Manifest Pydantic 定义
    ├─ manifest_validations.py         # 清单验证逻辑
    ├─ installation_utils.py           # 安装执行逻辑
    ├─ tasks.py                        # 异步安装任务
    └─ validators.py                   # URL 验证器
        │
        ▼
Webhook Domain (saleor/webhook)
    ├─ models.py                       # Webhook, WebhookEvent 模型
    └─ event_types.py                  # 事件类型定义
        │
        ▼
Permission Domain (saleor/permission)
    └─ enums.py                        # 权限枚举定义
```

## 总结

第三方应用清单接入是一个设计严谨的流程，在每个环节都有相应的安全和校验机制。核心设计原则包括：

1. **最小权限原则** - 禁止授予高风险权限，请求者不能越权
2. **原子性操作** - Token 发送失败时完整回滚
3. **深度防御** - 多层验证（结构、业务、权限）
4. **事件驱动** - 安装完成触发后续流程（Webhook、Plugin）
5. **异步处理** - 耗时操作通过 Celery 任务异步执行

理解这个流程对于开发第三方应用、排查安装问题或进行安全审计都具有重要意义。
