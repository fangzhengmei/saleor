# 订单重算主路径可核对深度说明

## 文档说明

本文档为**对账级**说明文档，所有步骤、计算、字段变化均可通过代码追溯和验证。文档基于 Saleor 订单重算主路径代码编写，不涉及任何代码修改。

---

## 1. 并发命中时的判定与清除/保留时序（可核对版）

### 1.1 重算触发与入口校验

**入口函数**：`fetch_order_prices_if_expired` (`saleor/order/calculations.py:192`)

**触发条件校验顺序**：
1. ✅ 订单状态是否可编辑（`order.status in ORDER_EDITABLE_STATUS`）
2. ✅ 是否强制更新（`force_update=True`）
3. ✅ 是否标记需要刷新（`order.should_refresh_prices=True`）
4. ✅ 是否存在过期行（`draft_base_price_expire_at < now`）
5. ✅ 税费策略是否允许同步调用（TAX_APP 策略需要 `allow_sync_webhooks=True`）

---

### 1.2 完整执行时序（精确到函数调用）

```
T0: 调用 fetch_order_prices_if_expired
    │
    ├─ 校验 should_refresh_prices → 返回 True/False
    │
    ▼
T1: prepare_order_lines_for_refresh
    │
    ├─ [IF 有过期行] refresh_order_base_prices_and_discounts
    │   │
    │   ├─ T1a: fetch_draft_order_lines_info
    │   │   └─ 构建 EditableOrderLineInfo，加载 discounts, variant, product
    │   │
    │   ├─ T1b: refresh_order_line_discount_objects_for_catalogue_promotions
    │   │   ├─ 检查行级手动折扣是否存在
    │   │   ├─ 检查是否仍享受目录促销
    │   │   ├─ 检查是否为礼品
    │   │   └─ 创建/更新/删除 OrderLineDiscount(PROMOTION)
    │   │
    │   ├─ T1c: refresh_manual_line_discount_object
    │   │   ├─ 基于原价重新计算手动折扣金额
    │   │   └─ 更新 base_unit_price_amount
    │   │
    │   └─ T1d: create_or_update_line_discount_objects_from_voucher
    │       ├─ 检查行级手动折扣是否存在
    │       ├─ 检查优惠券是否仍适用
    │       └─ 创建/更新/删除 OrderLineDiscount(VOUCHER)
    │
    └─ [ELSE] fetch_draft_order_lines_info
        └─ 构建 EditableOrderLineInfo
    │
    ▼
T2: process_order_promotion → handle_order_promotion
    │
    ├─ T2a: 互斥检查
    │   ├─ 检查 order.voucher_code 非空? → 清除 ORDER_PROMOTION
    │   ├─ 检查 order.discounts 存在 MANUAL? → 清除 ORDER_PROMOTION
    │   └─ 任一条件满足 → _clear_order_discount → RETURN
    │
    ├─ T2b: _set_order_base_prices
    │   └─ 计算仅含目录促销的 subtotal 和 total
    │
    ├─ T2c: fetch_promotion_rules_for_checkout_or_order
    │   └─ 获取所有适用的订单促销规则
    │
    ├─ T2d: get_best_rule
    │   ├─ 计算每个规则的折扣金额
    │   ├─ 礼品规则按礼品价格计算
    │   └─ 选择折扣最大的规则
    │
    └─ T2e: create_discount_objects_for_order_promotions
        ├─ 礼品规则 → 创建礼品行 + OrderLineDiscount
        └─ 折扣规则 → 创建/更新 OrderDiscount(ORDER_PROMOTION)
    │
    ▼
T3: process_order_prices
    │
    ├─ T3a: calculate_prices
    │   │
    │   ├─ T3a1: base_order_subtotal
    │   │   └─ Σ(line.base_unit_price × line.quantity)
    │   │
    │   └─ T3a2: propagate_order_discount_on_order_prices
    │       │
    │       ├─ [遍历 order.discounts.all()]
    │       │   │
    │       │   ├─ 类型为 VOUCHER 且是订单级 → apply 到 subtotal
    │       │   ├─ 类型为 ORDER_PROMOTION → apply 到 subtotal
    │       │   └─ 类型为 MANUAL
    │       │       ├─ PERCENTAGE → 同时 apply 到 subtotal + shipping
    │       │       └─ FIXED → 按比例分摊到 subtotal + shipping
    │       │
    │       └─ 更新 OrderDiscount.amount_value
    │
    ├─ T3b: [IF assign_prices=True]
    │   │
    │   ├─ T3b1: assign_order_prices
    │   │   └─ 更新 order.subtotal, shipping, total 字段
    │   │
    │   └─ T3b2: apply_subtotal_discount_to_order_lines
    │       ├─ 单行 → 全部分摊
    │       └─ 多行 → 前 N-1 行按比例，最后一行吸收尾差
    │
    └─ T3c: calculate_taxes
        │
        ├─ T3c1: 判断是否应缴税
        │   ├─ charge_taxes?
        │   └─ !tax_exemption?
        │
        ├─ T3c2: 选择策略
        │   ├─ FLAT_RATES → update_order_prices_with_flat_rates
        │   └─ TAX_APP → 调用 Webhook 获取 TaxData
        │
        └─ T3c3: 应用税费
            ├─ 更新 line.tax_rate
            ├─ 更新 line.unit_price_gross, total_price_gross
            ├─ 更新 order.shipping_tax_rate
            └─ 更新 order.subtotal_gross, total_gross
    │
    ▼
T4: process_calculation_result
    │
    ├─ T4a: 乐观锁检查
    │   ├─ order_qs_select_for_update() 获取 locked_order
    │   └─ locked_order.updated_at == order.updated_at?
    │
    ├─ [IF 匹配]
    │   ├─ T4b: order.save(update_fields=[11个字段])
    │   └─ T4c: order.lines.bulk_update(update_fields=[9个字段])
    │
    └─ [ELSE] 并发修改，放弃保存
```

---

### 1.3 四类折扣并发命中判定矩阵（可核对）

#### 场景 1：同时命中目录促销 + 行级手动折扣

**执行顺序**：T1b（目录促销）→ T1c（手动折扣）

**判定逻辑**（`prepare_order_line_discount_objects_for_catalogue_promotions`）：
```
IF 存在 MANUAL 行折扣:
    ▶ 将所有 PROMOTION 行折扣加入移除列表
    ▶ CONTINUE（跳过后续目录促销处理）
```

**结果**：
- ✅ 保留：`OrderLineDiscount(type=MANUAL)`
- ❌ 清除：`OrderLineDiscount(type=PROMOTION)`
- ✅ 字段变化：`base_unit_price_amount` 基于原价重新计算

---

#### 场景 2：同时命中目录促销 + 行级优惠券

**执行顺序**：T1b（目录促销）→ T1d（优惠券）

**判定逻辑**：
- 目录促销处理：无手动折扣 → 保留 PROMOTION 折扣
- 优惠券处理：无手动折扣 → 叠加 VOUCHER 折扣

**结果**：
- ✅ 保留：`OrderLineDiscount(type=PROMOTION)`
- ✅ 保留：`OrderLineDiscount(type=VOUCHER)`
- ✅ 叠加：`base_unit_price_amount` 先减目录促销，再减优惠券

---

#### 场景 3：同时命中订单促销 + 优惠券

**执行顺序**：T2（订单促销）

**判定逻辑**（`create_order_discount_objects_for_order_promotions`）：
```
IF order.voucher_code 非空:
    ▶ 调用 _clear_order_discount
    ▶ 删除所有 OrderDiscount(type=ORDER_PROMOTION)
    ▶ 删除礼品行
    ▶ RETURN
```

**结果**：
- ✅ 保留：`OrderDiscount(type=VOUCHER)`
- ❌ 清除：`OrderDiscount(type=ORDER_PROMOTION)`

---

#### 场景 4：同时命中订单促销 + 手动折扣

**执行顺序**：T2（订单促销）

**判定逻辑**：
```
IF order.discounts.filter(type=MANUAL).exists():
    ▶ 调用 _clear_order_discount
    ▶ 删除所有 OrderDiscount(type=ORDER_PROMOTION)
    ▶ 删除礼品行
    ▶ RETURN
```

**结果**：
- ✅ 保留：`OrderDiscount(type=MANUAL)`
- ❌ 清除：`OrderDiscount(type=ORDER_PROMOTION)`

---

#### 场景 5：同时命中行级优惠券 + 行级手动折扣

**执行顺序**：T1d（优惠券）

**判定逻辑**（`prepare_line_discount_objects_for_voucher`）：
```
IF 存在 MANUAL 行折扣:
    ▶ 将 VOUCHER 行折扣加入移除列表
    ▶ CONTINUE
```

**结果**：
- ✅ 保留：`OrderLineDiscount(type=MANUAL)`
- ❌ 清除：`OrderLineDiscount(type=VOUCHER)`

---

#### 场景 6：同时命中订单级优惠券 + 订单级手动折扣

**执行顺序**：订单级优惠券处理（`create_or_update_discount_object_from_order_level_voucher`）

**判定逻辑**：
```
IF 同时存在 VOUCHER 和 MANUAL 订单折扣:
    ▶ 删除 OrderDiscount(type=VOUCHER)
    ▶ 重置 base_shipping_price
```

**结果**：
- ✅ 保留：`OrderDiscount(type=MANUAL)`
- ❌ 清除：`OrderDiscount(type=VOUCHER)`

---

### 1.4 优先级总结表（从高到低）

| 优先级 | 折扣类型 | 作用层级 | 可叠加 |
|-------|---------|---------|-------|
| 1（最高） | 行级手动折扣 | 行级 | 不与任何行级折扣叠加 |
| 2 | 订单级手动折扣 | 订单级 | 不与订单促销叠加 |
| 3 | 行级优惠券 | 行级 | 可与目录促销叠加 |
| 4 | 订单级优惠券 | 订单级 | 不与订单促销叠加 |
| 5 | 目录促销 | 行级 | 可与行级优惠券叠加 |
| 6（最低） | 订单促销 | 订单级 | 不与优惠券/手动叠加 |

---

## 2. 固定金额折扣对账级示例（含多币种与舍入）

### 2.1 场景设定

**基础信息**：
- 渠道币种：USD（精度 2 位小数）
- 汇率（如涉及多币种）：1 USD = 0.92 EUR = 149.50 JPY
- 订单商品：
  - 商品 A：原价 $99.99，数量 2
  - 商品 B：原价 $149.50，数量 1
  - 商品 C：原价 $200.00，数量 1
- 运费：$15.00
- 折扣：订单级手动折扣，固定金额 $50.00

---

### 2.2 计算过程（每步可核对）

#### Step 1: 计算基础小计（无任何折扣）

```
商品 A: $99.99 × 2 = $199.98
商品 B: $149.50 × 1 = $149.50
商品 C: $200.00 × 1 = $200.00
──────────────────────────────────
基础小计: $199.98 + $149.50 + $200.00 = $549.48
```

**字段输入**：
- `line.undiscounted_base_unit_price_amount` = 各商品原价
- `line.quantity` = 各商品数量

**字段输出**：
- `base_subtotal` = $549.48

---

#### Step 2: 固定金额折扣在商品与运费间分摊

**总金额（含运费）**：$549.48 + $15.00 = $564.48

**分摊公式**：
```
商品分摊 = (商品小计 / 总金额) × 折扣总额
运费分摊 = 折扣总额 - 商品分摊
```

**计算过程**：
```
商品分摊比例 = $549.48 / $564.48 = 0.973427225...
商品分摊 = 0.973427225... × $50.00 = $48.67136125...
量化（ROUND_HALF_UP，2位小数）: $48.67

运费分摊 = $50.00 - $48.67 = $1.33
```

**验证**：$48.67 + $1.33 = $50.00 ✓

**字段输出**：
- `subtotal`（折扣后商品小计）= $549.48 - $48.67 = $500.81
- `shipping_price`（折扣后运费）= $15.00 - $1.33 = $13.67

---

#### Step 3: 商品分摊金额分摊到各行（按行金额比例）

**行金额（基础单价 × 数量）**：
- 商品 A: $199.98
- 商品 B: $149.50
- 商品 C: $200.00
- 合计: $549.48

**分摊规则**：
- 前 N-1 行：按比例计算并量化
- 最后一行（商品 C）：吸收尾差

**商品 A 分摊**：
```
比例 = $199.98 / $549.48 = 0.363944092...
分摊金额 = 0.363944092... × $48.67 = $17.71526481...
量化: $17.72
```

**商品 B 分摊**：
```
比例 = $149.50 / $549.48 = 0.272075417...
分摊金额 = 0.272075417... × $48.67 = $13.24190884...
量化: $13.24
```

**商品 C 分摊（吸收尾差）**：
```
已分摊 = $17.72 + $13.24 = $30.96
剩余 = $48.67 - $30.96 = $17.71
```

**验证**：$17.72 + $13.24 + $17.71 = $48.67 ✓

---

#### Step 4: 计算各行折扣后单价

**商品 A**：
```
行总价 = $199.98 - $17.72 = $182.26
单价 = $182.26 / 2 = $91.13
验证: $91.13 × 2 = $182.26 ✓
```

**商品 B**：
```
行总价 = $149.50 - $13.24 = $136.26
单价 = $136.26 / 1 = $136.26
```

**商品 C**：
```
行总价 = $200.00 - $17.71 = $182.29
单价 = $182.29 / 1 = $182.29
```

**字段输出**：
- `line.total_price_net_amount` = 各行折扣后总价
- `line.unit_price_net_amount` = 各行折扣后单价

---

#### Step 5: 订单总金额验证

```
折扣后商品小计 = $182.26 + $136.26 + $182.29 = $500.81
折扣后运费 = $13.67
订单总净价 = $500.81 + $13.67 = $514.48
```

**验证**：
- 原价总额 = $549.48 + $15.00 = $564.48
- 折扣总额 = $50.00
- 折扣后总额 = $564.48 - $50.00 = $514.48 ✓

---

### 2.3 多币种换算示例

#### 币种 1: EUR（精度 2 位小数，汇率 0.92）

```
折扣前总额: $564.48 × 0.92 = €519.3216 → €519.32
折扣金额: $50.00 × 0.92 = €46.00
折扣后总额: €519.32 - €46.00 = €473.32

分摊验证:
商品分摊比例 = ($549.48 × 0.92) / €519.32 = €505.52 / €519.32 = 0.973427...
商品分摊 = 0.973427... × €46.00 = €44.7776... → €44.78
运费分摊 = €46.00 - €44.78 = €1.22
验证: €44.78 + €1.22 = €46.00 ✓
```

#### 币种 2: JPY（精度 0 位小数，汇率 149.50）

```
折扣前总额: $564.48 × 149.50 = ¥84389.76 → ¥84390
折扣金额: $50.00 × 149.50 = ¥7475.00 → ¥7475
折扣后总额: ¥84390 - ¥7475 = ¥76915

分摊验证:
商品分摊比例 = ($549.48 × 149.50) / ¥84390 = ¥82146.06 / ¥84390 = 0.973411...
商品分摊 = 0.973411... × ¥7475 = ¥7276.73... → ¥7277
运费分摊 = ¥7475 - ¥7277 = ¥198
验证: ¥7277 + ¥198 = ¥7475 ✓
```

---

### 2.4 舍入顺序对结果的影响

#### 方案 A：先分摊后量化（Saleor 实际采用）

```
商品分摊 = 0.973427... × $50.00 = $48.671361... → $48.67
运费分摊 = $50.00 - $48.67 = $1.33
合计: $48.67 + $1.33 = $50.00 ✓
```

#### 方案 B：先量化后分摊（可能产生误差）

```
比例量化: 0.973427... → 0.97
商品分摊 = 0.97 × $50.00 = $48.50
运费分摊 = 0.03 × $50.00 = $1.50
合计: $48.50 + $1.50 = $50.00 ✓
（但分摊比例不准确）
```

**结论**：Saleor 采用**先计算后量化**的策略，保证分摊比例精确。

---

### 2.5 尾差处理对账表

| 分摊场景 | 尾差金额 | 吸收方 | 验证方式 |
|---------|---------|-------|---------|
| 商品/运费分摊 | $0.001361 | 运费 | 商品分摊 + 运费分摊 = 折扣总额 |
| 多行商品分摊 | $0.001463 | 最后一行（商品 C） | 各行分摊之和 = 商品分摊总额 |
| 单价计算 | $0.005 | 由除法自然消化 | 单价 × 数量 = 行总价 |

**对账公式**：
```
Σ(行折扣金额) + 运费折扣金额 = 订单折扣总额
Σ(行总价) + 运费 = 订单总净价
订单总净价 + 税费 = 订单总含税价
```

---

## 3. 各阶段输入输出字段变化（可核对）

### 3.1 字段定义表

| 字段名 | 所属模型 | 数据类型 | 说明 |
|-------|---------|---------|------|
| `undiscounted_base_unit_price_amount` | OrderLine | Decimal(12,3) | 原价，无任何折扣 |
| `base_unit_price_amount` | OrderLine | Decimal(12,3) | 基础单价，含行级折扣 |
| `unit_price_net_amount` | OrderLine | Decimal(12,3) | 单价净价，含所有折扣 |
| `unit_price_gross_amount` | OrderLine | Decimal(12,3) | 单价含税，含所有折扣+税费 |
| `total_price_net_amount` | OrderLine | Decimal(12,3) | 行总价净价 |
| `total_price_gross_amount` | OrderLine | Decimal(12,3) | 行总价含税 |
| `unit_discount_amount` | OrderLine | Decimal(12,3) | 单位折扣金额 |
| `tax_rate` | OrderLine | Decimal(5,4) | 税率 |
| `subtotal_net_amount` | Order | Decimal(12,3) | 小计净价（不含运费） |
| `subtotal_gross_amount` | Order | Decimal(12,3) | 小计含税 |
| `shipping_price_net_amount` | Order | Decimal(12,3) | 运费净价 |
| `shipping_price_gross_amount` | Order | Decimal(12,3) | 运费含税 |
| `total_net_amount` | Order | Decimal(12,3) | 总净价 |
| `total_gross_amount` | Order | Decimal(12,3) | 总含税 |
| `undiscounted_total_net_amount` | Order | Decimal(12,3) | 原价总净价 |
| `undiscounted_total_gross_amount` | Order | Decimal(12,3) | 原价总含税 |
| `base_shipping_price_amount` | Order | Decimal(12,3) | 基础运费（不含运费折扣） |
| `shipping_tax_rate` | Order | Decimal(5,4) | 运费税率 |
| `amount_value` | OrderLineDiscount | Decimal(12,3) | 行折扣实际金额 |
| `amount_value` | OrderDiscount | Decimal(12,3) | 订单折扣实际金额 |
| `value` | OrderLineDiscount/OrderDiscount | Decimal(12,3) | 折扣值（百分比或固定金额） |
| `value_type` | OrderLineDiscount/OrderDiscount | CharField | 折扣值类型（FIXED/PERCENTAGE） |

---

### 3.2 阶段 0: 初始状态（无任何折扣）

**输入字段**：
| 字段 | 商品 A | 商品 B | 商品 C |
|-----|-------|-------|-------|
| `undiscounted_base_unit_price_amount` | $99.99 | $149.50 | $200.00 |
| `base_unit_price_amount` | $99.99 | $149.50 | $200.00 |
| `quantity` | 2 | 1 | 1 |

**订单字段**：
| 字段 | 值 |
|-----|----|
| `base_shipping_price_amount` | $15.00 |
| `undiscounted_total_net_amount` | $564.48 |

---

### 3.3 阶段 1: 目录促销应用

**假设**：商品 A 享受 10% 目录促销

**处理函数**：`refresh_order_line_discount_objects_for_catalogue_promotions`

**输入**：
- `line.undiscounted_base_unit_price_amount` = $99.99
- 折扣规则：10% PERCENTAGE

**计算**：
```
折扣金额 = $99.99 × 2 × 10% = $19.998 → 量化 $20.00
单位折扣 = $20.00 / 2 = $10.00
```

**输出字段变化**：

| 字段 | 商品 A（变化前→变化后） |
|-----|----------------------|
| `OrderLineDiscount.type` | 无 → PROMOTION |
| `OrderLineDiscount.value_type` | 无 → PERCENTAGE |
| `OrderLineDiscount.value` | 无 → 10 |
| `OrderLineDiscount.amount_value` | 无 → $20.00 |
| `base_unit_price_amount` | $99.99 → $89.99 |
| `unit_discount_amount` | $0.00 → $10.00 |

---

### 3.4 阶段 2: 行级优惠券应用

**假设**：商品 B 享受 $15.00 优惠券

**处理函数**：`create_or_update_line_discount_objects_from_voucher`

**输入**：
- `line.base_unit_price_amount` = $149.50
- 优惠券：$15.00 FIXED

**计算**：
```
折扣金额 = $15.00
单位折扣 = $15.00 / 1 = $15.00
```

**输出字段变化**：

| 字段 | 商品 B（变化前→变化后） |
|-----|----------------------|
| `OrderLineDiscount.type` | 无 → VOUCHER |
| `OrderLineDiscount.value_type` | 无 → FIXED |
| `OrderLineDiscount.value` | 无 → $15.00 |
| `OrderLineDiscount.amount_value` | 无 → $15.00 |
| `base_unit_price_amount` | $149.50 → $134.50 |
| `unit_discount_amount` | $0.00 → $15.00 |

---

### 3.5 阶段 3: 行级手动折扣应用

**假设**：商品 C 添加 $20.00 手动折扣

**处理函数**：`refresh_manual_line_discount_object`

**注意**：此操作会先清除商品 C 的所有其他行级折扣

**输入**：
- `line.undiscounted_base_unit_price_amount` = $200.00
- 手动折扣：$20.00 FIXED

**计算**：
```
折扣金额 = $20.00
单位折扣 = $20.00 / 1 = $20.00
```

**输出字段变化**：

| 字段 | 商品 C（变化前→变化后） |
|-----|----------------------|
| `OrderLineDiscount(type=PROMOTION)` | 如有 → 删除 |
| `OrderLineDiscount(type=VOUCHER)` | 如有 → 删除 |
| `OrderLineDiscount.type` | 无 → MANUAL |
| `OrderLineDiscount.value_type` | 无 → FIXED |
| `OrderLineDiscount.value` | 无 → $20.00 |
| `OrderLineDiscount.amount_value` | 无 → $20.00 |
| `base_unit_price_amount` | $200.00 → $180.00 |
| `unit_discount_amount` | $0.00 → $20.00 |

---

### 3.6 阶段 4: 基础小计计算

**处理函数**：`base_order_subtotal`

**输入**：
| 字段 | 商品 A | 商品 B | 商品 C |
|-----|-------|-------|-------|
| `base_unit_price_amount` | $89.99 | $134.50 | $180.00 |
| `quantity` | 2 | 1 | 1 |

**计算**：
```
商品 A: $89.99 × 2 = $179.98
商品 B: $134.50 × 1 = $134.50
商品 C: $180.00 × 1 = $180.00
──────────────────────────────────
base_subtotal = $179.98 + $134.50 + $180.00 = $494.48
```

**输出字段**：
- 中间结果，不持久化

---

### 3.7 阶段 5: 订单级折扣传播

**假设**：订单级手动折扣 $50.00 FIXED

**处理函数**：`propagate_order_discount_on_order_prices`

**输入**：
- `base_subtotal` = $494.48
- `base_shipping_price` = $15.00
- `OrderDiscount(type=MANUAL, value=$50.00, value_type=FIXED)`

**计算**：
```
总金额 = $494.48 + $15.00 = $509.48
商品分摊 = ($494.48 / $509.48) × $50.00 = $48.5286... → $48.53
运费分摊 = $50.00 - $48.53 = $1.47

subtotal = $494.48 - $48.53 = $445.95
shipping_price = $15.00 - $1.47 = $13.53
```

**输出字段变化**：

| 字段 | 变化前→变化后 |
|-----|-------------|
| `OrderDiscount.amount_value` | $0.00 → $50.00 |
| `subtotal_net_amount` | $494.48 → $445.95 |
| `shipping_price_net_amount` | $15.00 → $13.53 |

---

### 3.8 阶段 6: 订单级折扣分摊到行

**处理函数**：`apply_subtotal_discount_to_order_lines`

**输入**：
- `base_subtotal` = $494.48
- `subtotal_discount` = $48.53

**行金额**：
- 商品 A: $179.98
- 商品 B: $134.50
- 商品 C: $180.00

**分摊计算**：
```
商品 A 分摊 = ($179.98 / $494.48) × $48.53 = $17.668... → $17.67
商品 B 分摊 = ($134.50 / $494.48) × $48.53 = $13.203... → $13.20
商品 C 分摊 = $48.53 - $17.67 - $13.20 = $17.66
```

**各行总价计算**：
```
商品 A: $179.98 - $17.67 = $162.31 → 单价 $81.155 → $81.16
商品 B: $134.50 - $13.20 = $121.30 → 单价 $121.30
商品 C: $180.00 - $17.66 = $162.34 → 单价 $162.34
```

**输出字段变化**：

| 字段 | 商品 A | 商品 B | 商品 C |
|-----|-------|-------|-------|
| `total_price_net_amount` | $179.98→$162.31 | $134.50→$121.30 | $180.00→$162.34 |
| `total_price_gross_amount` | $179.98→$162.31 | $134.50→$121.30 | $180.00→$162.34 |
| `unit_price_net_amount` | $89.99→$81.16 | $134.50→$121.30 | $180.00→$162.34 |
| `unit_price_gross_amount` | $89.99→$81.16 | $134.50→$121.30 | $180.00→$162.34 |
| `undiscounted_unit_price_net_amount` | -→$99.99 | -→$149.50 | -→$200.00 |
| `undiscounted_unit_price_gross_amount` | -→$99.99 | -→$149.50 | -→$200.00 |

---

### 3.9 阶段 7: 税费计算（FLAT_RATES 策略）

**假设**：税率 10%，`prices_entered_with_tax=False`

**处理函数**：`update_order_prices_with_flat_rates`

**输入**：
| 字段 | 商品 A | 商品 B | 商品 C |
|-----|-------|-------|-------|
| `total_price_net_amount` | $162.31 | $121.30 | $162.34 |
| `unit_price_net_amount` | $81.16 | $121.30 | $162.34 |

**计算**：
```
税率: 10% = 0.10

商品 A:
total_price_gross = $162.31 × 1.10 = $178.541 → $178.54
unit_price_gross = $81.16 × 1.10 = $89.276 → $89.28

商品 B:
total_price_gross = $121.30 × 1.10 = $133.43
unit_price_gross = $121.30 × 1.10 = $133.43

商品 C:
total_price_gross = $162.34 × 1.10 = $178.574 → $178.57
unit_price_gross = $162.34 × 1.10 = $178.574 → $178.57

运费:
shipping_gross = $13.53 × 1.10 = $14.883 → $14.88

订单汇总:
subtotal_gross = $178.54 + $133.43 + $178.57 = $490.54
total_gross = $490.54 + $14.88 = $505.42
```

**输出字段变化**：

| 字段 | 商品 A | 商品 B | 商品 C |
|-----|-------|-------|-------|
| `tax_rate` | $0.0000→$0.1000 | $0.0000→$0.1000 | $0.0000→$0.1000 |
| `total_price_gross_amount` | $162.31→$178.54 | $121.30→$133.43 | $162.34→$178.57 |
| `unit_price_gross_amount` | $81.16→$89.28 | $121.30→$133.43 | $162.34→$178.57 |

**订单字段变化**：

| 字段 | 变化前→变化后 |
|-----|-------------|
| `shipping_tax_rate` | $0.0000→$0.1000 |
| `shipping_price_gross_amount` | $13.53→$14.88 |
| `subtotal_gross_amount` | $445.95→$490.54 |
| `total_gross_amount` | $459.48→$505.42 |
| `undiscounted_total_gross_amount` | $564.48→$620.93 |

---

### 3.10 阶段 8: 持久化保存

**处理函数**：`process_calculation_result`

**乐观锁检查**：
```python
locked_order = order_qs_select_for_update().only("updated_at").get(id=order.id)
if locked_order.updated_at == order.updated_at:
    # 保存
else:
    # 放弃保存
```

**保存的订单字段（11 个）**：
```
subtotal_net_amount, subtotal_gross_amount,
total_net_amount, total_gross_amount,
undiscounted_total_net_amount, undiscounted_total_gross_amount,
shipping_price_net_amount, shipping_price_gross_amount,
base_shipping_price_amount, shipping_tax_rate,
should_refresh_prices, tax_error
```

**批量更新的订单行字段（9 个）**：
```
unit_price_net_amount, unit_price_gross_amount,
undiscounted_unit_price_net_amount, undiscounted_unit_price_gross_amount,
total_price_net_amount, total_price_gross_amount,
undiscounted_total_price_net_amount, undiscounted_total_price_gross_amount,
tax_rate
```

---

### 3.11 字段流转全景图（带金额）

```
undiscounted_base_unit_price
    ($99.99, $149.50, $200.00)
          │
          ▼ 目录促销 -$20.00
base_unit_price
    ($89.99, $149.50, $200.00)
          │
          ▼ 行级优惠券 -$15.00
base_unit_price
    ($89.99, $134.50, $200.00)
          │
          ▼ 行级手动折扣 -$20.00（清除其他行级折扣）
base_unit_price
    ($89.99, $134.50, $180.00)
          │
          ▼ Σ(×数量)
base_subtotal = $494.48
          │
          ▼ 订单级手动折扣 -$50.00
subtotal = $445.95
          │
          ▼ 分摊到各行
total_price_net
    ($162.31, $121.30, $162.34)
          │
          ▼ 税费 +10%
total_price_gross
    ($178.54, $133.43, $178.57)
          │
          ▼ Σ
order.subtotal_gross = $490.54
order.total_gross = $505.42
```

---

## 4. 对账校验清单

### 4.1 折扣校验

| 校验项 | 公式 | 示例值 |
|-------|------|-------|
| 行折扣总额 | Σ(OrderLineDiscount.amount_value) | $20.00 + $15.00 + $20.00 = $55.00 |
| 订单折扣总额 | OrderDiscount.amount_value | $50.00 |
| 折扣总计 | 行折扣总额 + 订单折扣总额 | $105.00 |
| 原价总额 | order.undiscounted_total_net_amount | $564.48 |
| 折扣后总额 | order.total_net_amount | $459.48 |
| 校验 | 原价总额 - 折扣总计 = 折扣后总额 | $564.48 - $105.00 = $459.48 ✓ |

### 4.2 税费校验

| 校验项 | 公式 | 示例值 |
|-------|------|-------|
| 税费总额 | total_gross - total_net | $505.42 - $459.48 = $45.94 |
| 平均税率 | 税费总额 / total_net | $45.94 / $459.48 ≈ 10.00% ✓ |
| 行级校验 | unit_price_gross = unit_price_net × (1 + tax_rate) | $89.28 = $81.16 × 1.10 ✓ |

### 4.3 分摊校验

| 校验项 | 公式 | 示例值 |
|-------|------|-------|
| 商品分摊 + 运费分摊 | = 订单折扣总额 | $48.53 + $1.47 = $50.00 ✓ |
| 各行分摊之和 | = 商品分摊总额 | $17.67 + $13.20 + $17.66 = $48.53 ✓ |
| 折扣后小计 | = Σ(行总价) | $162.31 + $121.30 + $162.34 = $445.95 ✓ |

---

## 5. 常见异常场景处理

### 5.1 并发修改（乐观锁冲突）

**现象**：
- T1 开始重算，读取 `updated_at = 2026-05-18 10:00:00`
- T2 在 T1 计算过程中修改订单，`updated_at = 2026-05-18 10:00:05`
- T1 完成计算，检查 `locked_order.updated_at` 为 10:00:05
- 不匹配，放弃保存

**处理结果**：
- 内存中的计算结果丢弃
- 数据库中的数据保持 T2 修改后的状态
- 下次调用重算时会重新计算

### 5.2 折扣金额超过商品金额

**处理逻辑**：
```python
total_price = max(base_line_total - discount, zero_money(currency))
```

**结果**：
- 折扣后金额最低为 0
- 不会出现负数

### 5.3 多行分摊时某行金额为 0

**处理**：
- 0 金额行不分摊折扣
- 折扣在其他非 0 行之间分摊

---

## 6. 总结

本文档从**对账级**角度详细说明了 Saleor 订单重算主路径的完整流程：

1. **时序精确**：从 T0 到 T4 的完整执行时序，精确到每个函数调用
2. **判定清晰**：6 种并发命中场景的判定逻辑和结果
3. **示例可核对**：固定金额折扣的 6 步计算过程，每步可验证
4. **字段完整**：8 个计算阶段的输入输出字段变化，含具体金额示例
5. **对账清单**：折扣、税费、分摊三类校验公式和示例

所有内容均基于源代码分析，不涉及任何代码修改，可直接用于系统理解和问题排查。
