# 订单计算链路分析报告

## 1. 概述

Saleor 订单计算链路是一个多阶段、分层处理的价格计算体系，负责将商品原价、各类折扣规则、优惠券、运费和税费等多个价格组件整合，最终生成订单的应付金额。整个链路遵循严格的执行顺序，确保计算结果的准确性和一致性。

---

## 2. 折扣类型体系

### 2.1 折扣类型分类

| 折扣类型 | 说明 | 作用层级 |
|---------|------|---------|
| **CATALOGUE_PROMOTION** | 目录促销，基于商品的折扣规则 | 订单行（Line Level） |
| **ORDER_PROMOTION** | 订单促销，基于订单整体的折扣规则 | 订单级（Order Level） |
| **VOUCHER** | 优惠券，用户输入优惠码获得的折扣 | 订单级/行级 |
| **MANUAL** | 手动折扣，管理员手动添加的折扣 | 订单级/行级 |

### 2.2 优惠券类型

| 优惠券类型 | 说明 | 作用范围 |
|-----------|------|---------|
| **ENTIRE_ORDER** | 整单折扣 | 订单级 |
| **SPECIFIC_PRODUCT** | 指定商品折扣 | 行级 |
| **SHIPPING** | 运费折扣 | 运费 |

### 2.3 折扣值类型

| 折扣值类型 | 说明 |
|-----------|------|
| **FIXED** | 固定金额折扣 |
| **PERCENTAGE** | 百分比折扣 |

---

## 3. 执行顺序与互斥/叠加规则

### 3.1 整体执行流水线

```
┌─────────────────────────────────────────────────────────┐
│                    价格计算总流程                        │
├─────────────────────────────────────────────────────────┤
│  1. 基础价格计算（原价）                                │
│         ↓                                               │
│  2. 目录促销（Catalogue Promotion）                     │
│         ↓                                               │
│  3. 特定商品优惠券（Specific Product Voucher）          │
│         ↓                                               │
│  4. 行级手动折扣（Manual Line Discount）                │
│         ↓                                               │
│  5. 订单级折扣（Order Promotion/Voucher/Manual）        │
│         ↓                                               │
│  6. 运费计算与运费折扣                                  │
│         ↓                                               │
│  7. 税费计算                                            │
└─────────────────────────────────────────────────────────┘
```

### 3.2 行级折扣执行顺序（Line Level）

**执行顺序**：目录促销 → 特定商品优惠券 → 手动行折扣

**互斥规则**：
- 手动行折扣（MANUAL）与其他行级折扣**互斥**
- 当存在手动行折扣时，自动移除目录促销折扣
- 目录促销折扣同一行只能应用一个（取最优）

**叠加规则**：
- 目录促销与特定商品优惠券**可叠加**
- 多个目录促销规则取最优折扣（不叠加）

### 3.3 订单级折扣执行顺序（Order Level）

**执行顺序**：订单促销 → 优惠券 → 手动折扣

**互斥规则**：
- 订单促销（ORDER_PROMOTION）与优惠券/手动折扣**互斥**
- 当存在优惠券或手动折扣时，自动清除订单促销
- 优惠券与手动折扣**可叠加**

**判定逻辑**（`create_order_discount_objects_for_order_promotions`）：
```python
if order.voucher_code or order.discounts.filter(type=DiscountType.MANUAL):
    _clear_order_discount(order, lines_info)
    return
```

### 3.4 折扣类型互斥矩阵

| 折扣类型 | 目录促销 | 订单促销 | 优惠券 | 手动折扣 |
|---------|---------|---------|--------|---------|
| **目录促销** | - | ✅ | ✅ | ❌ |
| **订单促销** | ✅ | - | ❌ | ❌ |
| **优惠券** | ✅ | ❌ | - | ✅ |
| **手动折扣** | ❌ | ❌ | ✅ | - |

✅ = 可叠加 | ❌ = 互斥

---

## 4. 适用范围匹配判定

### 4.1 目录促销匹配

**判定时机**：商品加入购物车时预计算，订单计算时重新校验

**匹配维度**：
1. **商品维度**：直接指定商品、商品变体
2. **分类维度**：商品分类（Category）
3. **集合维度**：商品集合（Collection）
4. **产品类型维度**：产品类型（ProductType）
5. **属性维度**：商品属性值

**匹配方式**：
- 使用谓词表达式（Predicate）进行条件匹配
- 支持 AND/OR 逻辑组合
- 基于数据库查询进行高效筛选

### 4.2 订单促销匹配

**判定时机**：订单价格重新计算时

**匹配维度**：
1. **订单金额阈值**：最小订单金额
2. **商品数量阈值**：最小商品数量
3. **客户分组**：特定客户组
4. **配送地址**：特定国家/地区

**最佳规则选择**：
- 收集所有符合条件的订单促销规则
- 计算每个规则的折扣金额
- 选择折扣金额最大的规则
- 礼品规则（GIFT）也参与比较，以礼品价格作为折扣金额

### 4.3 优惠券匹配

**判定时机**：用户输入优惠码时

**匹配维度**：
1. **有效性校验**：
   - 优惠码是否存在且激活
   - 优惠券是否在有效期内
   - 使用次数是否超限
   - 是否限制单客单用

2. **适用范围校验**：
   - **ENTIRE_ORDER**：无额外限制
   - **SPECIFIC_PRODUCT**：订单中是否包含指定商品/分类/集合
   - **SHIPPING**：订单是否需要配送

3. **渠道限制**：优惠券是否适用于当前渠道

---

## 5. 价格层级协作关系

### 5.1 价格字段体系

| 价格字段 | 说明 | 包含折扣 |
|---------|------|---------|
| `undiscounted_base_unit_price` | 原价（基础单价） | ❌ 无 |
| `base_unit_price` | 基础单价 | ✅ 行级折扣 |
| `unit_price_net` | 单价（净价） | ✅ 所有折扣 |
| `unit_price_gross` | 单价（含税） | ✅ 所有折扣 + 税费 |

### 5.2 订单行价格计算

**公式**：
```
base_unit_price = undiscounted_base_unit_price - 行级折扣总额 / 数量
unit_price_net = base_unit_price - 订单级折扣分摊 / 数量
unit_price_gross = unit_price_net * (1 + 税率)
```

### 5.3 订单总额计算

**公式**：
```
订单小计（不含运费）= Σ(行数量 × base_unit_price)
订单小计（含订单折扣）= 订单小计（不含运费） - 订单级折扣总额
订单运费 = 基础运费 - 运费折扣
订单总净价 = 订单小计（含订单折扣） + 订单运费（净价）
订单总含税价 = 订单总净价 + 订单税费
```

### 5.4 订单级折扣分摊

**折扣分摊规则**（`propagate_order_discount_on_order_lines_prices`）：

1. **单行订单**：折扣全部应用到该行
2. **多行订单**：按各行金额比例分摊
   - 前 N-1 行：按比例计算并量化
   - 最后一行：应用剩余折扣金额（避免四舍五入误差）

**公式**：
```
第 i 行折扣 = (第 i 行金额 / 订单小计) × 订单级折扣总额
最后一行折扣 = 订单级折扣总额 - Σ(前 N-1 行折扣)
```

### 5.5 手动固定金额折扣分摊

当手动折扣为固定金额（FIXED）且作用于订单级时，需要在商品小计和运费之间分摊：

```
商品小计分摊 = (商品小计 / (商品小计 + 运费)) × 折扣总额
运费分摊 = 折扣总额 - 商品小计分摊
```

---

## 6. 多币种金额换算与精度处理

### 6.1 币种体系

**币种绑定层级**：
1. **渠道（Channel）**：每个渠道绑定一种货币（`currency_code`）
2. **商品价格**：每个商品在不同渠道有独立的定价（`ProductVariantChannelListing`）
3. **促销规则**：固定金额折扣的金额与渠道币种绑定
4. **订单**：订单币种与创建时的渠道币种一致

### 6.2 精度处理机制

**核心函数**：`quantize_price(price, currency)`

**实现原理**：
- 使用 `babel.numbers.get_currency_precision(currency)` 获取币种精度
- 根据币种精度对金额进行四舍五入量化
- 支持 `Money`、`TaxedMoney`、`Decimal`、`TaxedMoneyRange` 等多种价格类型

**示例**：
```python
# 美元（USD）精度为 2 位小数
quantize_price(Decimal("10.125"), "USD")  # 10.13

# 日元（JPY）精度为 0 位小数
quantize_price(Decimal("1000.5"), "JPY")  # 1001
```

### 6.3 精度应用场景

1. **折扣计算**：每次应用折扣后量化
2. **金额分摊**：分摊后对每行金额量化
3. **税费计算**：税费计算后量化
4. **数据持久化**：保存到数据库前量化

### 6.4 避免精度损失的设计

1. **使用 Decimal 类型**：所有金额计算使用 Python 的 `Decimal` 类型
2. **延迟量化**：在计算链的最后环节进行量化
3. **余数处理**：多行分摊时最后一行吸收余数
4. **数据库字段**：使用 `DecimalField(max_digits=12, decimal_places=3)` 存储

---

## 7. 税费计算协作

### 7.1 税费计算时机

税费计算在**所有折扣应用完成后**进行，确保税费基于折扣后的价格计算。

### 7.2 税费计算策略

| 策略 | 说明 |
|------|------|
| **FLAT_RATES** | 固定税率，基于税率配置计算 |
| **TAX_APP** | 税务应用集成，通过 Webhook 调用外部税务服务 |

### 7.3 含税价与不含税价

**配置项**：`tax_configuration.prices_entered_with_tax`

- **True**：商品价格为含税价，需要倒推净价
- **False**：商品价格为净价，税费额外计算

### 7.4 税费计算流程

```
折扣后价格
      ↓
判断是否应缴税（charge_taxes && !tax_exemption）
      ↓
选择税费计算策略（FLAT_RATES / TAX_APP）
      ↓
计算各行税费
      ↓
计算运费税费
      ↓
汇总订单总税费
      ↓
更新订单和行的含税价格字段
```

---

## 8. 并发安全与事务处理

### 8.1 行级锁机制

**关键操作加锁**：
- 订单促销折扣创建/更新
- 礼品行创建
- 目录折扣更新

**锁对象**：`order_qs_select_for_update()`

### 8.2 乐观锁检测

通过 `updated_at` 字段检测并发修改：

```python
if locked_order.updated_at == order.updated_at:
    order.save(update_fields=[...])
```

如果 `updated_at` 不匹配，说明订单在计算过程中已被其他请求修改，当前计算结果将被丢弃。

### 8.3 幂等性设计

- 折扣创建使用 `get_or_create` 避免重复
- 价格重新计算是幂等操作
- 优惠券使用增加使用原子更新（`F("used") + 1`）

---

## 9. 关键数据结构

### 9.1 EditableOrderLineInfo

订单行计算上下文，包含：
- 行对象（OrderLine）
- 已应用的折扣列表
- 适用的促销规则信息
- 渠道定价信息
- 优惠券信息

### 9.2 OrderDiscount

订单级折扣记录：
- 折扣类型（ORDER_PROMOTION / VOUCHER / MANUAL）
- 折扣值类型（FIXED / PERCENTAGE）
- 折扣值
- 实际折扣金额
- 关联的促销规则/优惠券

### 9.3 OrderLineDiscount

行级折扣记录：
- 关联的订单行
- 折扣类型（PROMOTION / VOUCHER / MANUAL）
- 折扣值和实际金额
- 关联的促销规则/优惠券

---

## 10. 扩展点与自定义

### 10.1 新增折扣类型

1. 在 `DiscountType` 枚举中添加新类型
2. 实现折扣应用逻辑
3. 在 `propagate_order_discount_on_order_prices` 中添加处理分支
4. 更新互斥规则

### 10.2 自定义折扣计算

通过插件（Plugins）系统扩展：
- `calculate_order_line_unit`
- `calculate_order_line_total`
- `calculate_order_shipping`
- `calculate_order_total`

### 10.3 自定义税费计算

实现税务应用（Tax App）：
- 注册 `order.calculate_taxes` Webhook
- 返回 `TaxData` 结构
- 支持自定义税费规则和逻辑

---

## 11. 总结

Saleor 订单计算链路通过**分层设计**、**明确的执行顺序**和**严格的互斥规则**，确保了复杂折扣场景下的价格计算准确性。核心设计要点包括：

1. **流水线架构**：各折扣类型按固定顺序执行，避免逻辑混乱
2. **互斥优先**：优先保证简单明确的互斥规则，减少不确定性
3. **精度优先**：全程使用 Decimal 类型，关键节点进行量化
4. **并发安全**：行级锁 + 乐观锁的双重保障
5. **可扩展性**：插件系统支持自定义价格和税费计算

这套体系既满足了电商场景的灵活性需求，又保证了计算结果的可靠性和一致性。
