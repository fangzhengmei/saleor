# Saleor 第三方应用清单接入流程概念说明

**文档版本**: v1.1
**最后更新**: 2026-05-18
**适用范围**: Saleor 第三方应用通过 Manifest 清单文件接入的完整流程

---

## 概述

第三方应用通过清单文件（Manifest）接入 Saleor 是一个涉及多模块协作的完整流程，包括清单获取、格式校验、权限评估、应用注册、Token 颁发和 Webhook 通道配置等核心环节。

---

## 一、清单文件（Manifest）结构与验证

### 1.1 核心字段定义

清单文件是一个 JSON 格式的描述文件，定义了第三方应用的元数据。以下是核心字段：

| 字段 | 类型 | 必要性 | 说明 |
|------|------|--------|------|
| `id` | string | **必填** | 应用唯一标识符 |
| `name` | string | **必填** | 应用名称 |
| `version` | string | **必填** | 应用版本号 |
| `permissions` | string[] | 可选 | 应用请求的权限名称列表 |
| `appUrl` | string | 可选 | 应用主页 URL，用于解析相对路径扩展 |
| `tokenTargetUrl` | string | 可选 | 接收安装 Token 的回调地址 |
| `extensions` | array | 可选 | UI 扩展点配置 |
| `webhooks` | array | 可选 | Webhook 订阅配置 |
| `requiredSaleorVersion` | string | 可选 | 要求的 Saleor 版本范围 |
| `audience` | string | 可选 | JWT audience 声明 |
| `author` | string | 可选 | 应用作者信息 |
| `brand` | object | 可选 | 品牌 Logo 等视觉元素 |

### 1.2 验证层级

清单文件的验证分为两个阶段：

**第一阶段：结构验证（Pydantic Schema - `ManifestSchema`）**
- 必填字段存在性检查
- 字段类型校验
- URL 格式合法性校验
- Brand Logo 图片格式验证

**第二阶段：业务逻辑验证（`clean_manifest_data`）**
- 权限有效性校验
- 权限范围限制
- 应用标识符唯一性检查
- Saleor 版本兼容性检查
- 扩展点 URL 校验
- Webhook 事件类型与订阅查询有效性

### 1.3 各字段 URL 协议白名单

不同字段使用不同的 URL 验证器，允许的协议范围不同：

| 字段 | 验证器配置 | 允许的协议 |
|------|-----------|-----------|
| `manifest_url`（安装输入） | `AppURLValidator()` | 继承 Django 默认 |
| `tokenTargetUrl` | `AppURLValidator()` | 继承 Django 默认 |
| `appUrl` | `AppURLValidator()` | 继承 Django 默认 |
| `extensions[].url`（绝对路径） | `AppURLValidator()` | 继承 Django 默认 |
| `webhooks[].targetUrl` | `AppURLValidator(schemes=["http", "https", "awssqs", "gcpubsub"])` | `http`, `https`, `awssqs`, `gcpubsub` |
| `brand.logo.default` | `image_url_validator` | 继承 Django 默认 |

---

## 二、应用注册完整链路

### 2.1 端到端流程图

```
用户触发 appInstall Mutation
    │
    ▼
[GraphQL 层校验]
    ├─ 1. 检查请求者是否拥有 MANAGE_APPS 权限
    ├─ 2. 检查请求者是否为 staff
    ├─ 3. 校验 manifest_url 格式合法性
    ├─ 4. 校验待授予权限不包含 MANAGE_APPS
    ├─ 5. 校验请求者拥有待授予的所有权限
    └─ 6. 创建 AppInstallation 记录
            │
            ▼
[Celery 异步任务：install_app_task]
    ├─ 1. 根据 manifest_url 拉取 Manifest JSON
    ├─ 2. 将 AppInstallation.permissions 转换为权限名称列表
    ├─ 3. 覆盖 Manifest.permissions 为实际授予的权限
    ├─ 4. 执行完整的 Manifest 验证
    ├─ 5. 创建 App 记录（is_installed=False）
    ├─ 6. 将权限关联到 App.permissions
    ├─ 7. 创建 AppExtension 记录（如需要）
    ├─ 8. 批量创建 Webhook 记录
    ├─ 9. 批量创建 WebhookEvent 记录
    ├─ 10. 如声明了 tokenTargetUrl：
    │     ├─ 生成 AppToken
    │     ├─ POST 明文 Token 到 tokenTargetUrl
    │     └─ 失败则删除 App 并回滚
    ├─ 11. 标记 App.is_installed = True
    ├─ 12. 删除 AppInstallation 记录
    ├─ 13. 触发 PluginManager.app_installed 事件
    └─ 14. 异步获取 Brand Logo 数据
```

### 2.2 关键入口点

**GraphQL Mutation:**
- `appFetchManifest` - 获取并校验清单，不执行安装
- `appInstall` - 触发完整安装流程

**核心模块:**
- `saleor/app/manifest_schema.py` - Manifest Pydantic 结构定义
- `saleor/app/manifest_validations.py` - 清单业务逻辑验证
- `saleor/app/installation_utils.py` - 安装执行逻辑
- `saleor/app/tasks.py` - 异步任务处理

### 2.3 前后依赖关系

```
┌─────────────────────────────────────────────────────────┐
│           GraphQL 请求层 (saleor/graphql/app)            │
└─────────────────────────────────────────────────────────┘
    │
    ├─ 依赖: PermissionEnum → 权限枚举值
    ├─ 依赖: get_user_or_app_from_context → 获取请求者
    ├─ 依赖: ensure_app_permissions_allowed → 权限黑名单检查
    ├─ 依赖: ensure_can_manage_permissions → 请求者权限校验
    └─ 依赖: AppInstallation 模型 → 持久化安装任务
            │
            ▼
┌─────────────────────────────────────────────────────────┐
│           异步任务层 (saleor/app/tasks.py)              │
└─────────────────────────────────────────────────────────┘
    │
    ├─ 依赖: AppInstallation → 读取安装任务
    ├─ 依赖: install_app() → 执行实际安装
    └─ 依赖: JobStatus → 标记任务状态
            │
            ▼
┌─────────────────────────────────────────────────────────┐
│         安装核心逻辑 (saleor/app/installation_utils.py)  │
└─────────────────────────────────────────────────────────┘
    │
    ├─ 依赖: fetch_manifest() → 拉取清单
    ├─ 依赖: clean_manifest_data() → 完整清单验证
    ├─ 依赖: App 模型 → 创建应用记录
    ├─ 依赖: AppToken 模型 → 生成访问令牌
    ├─ 依赖: AppExtension 模型 → 创建 UI 扩展
    ├─ 依赖: Webhook / WebhookEvent 模型 → 创建 Webhook
    ├─ 依赖: send_app_token() → 发送 Token 到回调
    └─ 依赖: PluginsManager → 触发插件事件
```

---

## 三、权限授予机制

### 3.1 权限格式与校验位置

权限校验在不同阶段使用两种不同的格式：

| 阶段 | 权限格式 | 校验函数 | 检查内容 |
|------|---------|---------|---------|
| GraphQL Mutation 输入 | Dotted codename<br>例：`"app.manage_apps"` | `saleor/graphql/app/utils.py`<br>`ensure_app_permissions_allowed()` | 检查 `AppPermission.MANAGE_APPS.value` |
| Manifest 验证 | 权限名称<br>例：`"MANAGE_APPS"` | `saleor/app/manifest_validations.py`<br>`_ensure_app_permissions_allowed()` | 检查 `AppPermission.MANAGE_APPS.name` |

### 3.2 权限范围约束

**全局禁止权限:**
- `MANAGE_APPS` - 禁止授予任何第三方应用

**请求者权限校验:**
1. 执行安装的用户必须拥有 `MANAGE_APPS` 权限
2. 用户必须是 staff 用户
3. 用户不能授予自己不拥有的权限
4. 超级用户不受第 3 条限制

**权限校验流程:**
```
GraphQL 输入 permissions (dotted codename 格式)
    │
    ├─ ▶ ensure_app_permissions_allowed() 检查 MANAGE_APPS.value
    ├─ ▶ get_permissions() 转换为 DB Permission 对象
    └─ ▶ ensure_can_manage_permissions() 检查请求者权限范围
            │
            ▼
存储到 AppInstallation.permissions
            │
            ▼
异步任务读取 AppInstallation.permissions
    │
    └─ ▶ get_permission_names() 转换为权限名称列表
            │
            ▼
覆盖 Manifest.permissions 字段
    │
    └─ ▶ clean_manifest_data()
         ├─ _ensure_app_permissions_allowed() 检查 MANAGE_APPS.name
         └─ _clean_permissions() 验证权限存在性
```

> 权限以用户在 Mutation 中传入的 permissions 参数为准，Manifest 中的 permissions 会被覆盖。

### 3.3 权限层级

应用权限分为两个层级：

1. **应用级权限** (`App.permissions`)
   - 定义应用整体可访问的资源范围
   - 在 Mutation 输入的 `permissions` 字段中声明

2. **扩展点级权限** (`AppExtension.permissions`)
   - 单个 UI 扩展点所需的权限
   - 应为应用级权限的子集
   - 在 Manifest 的 `extensions[].permissions` 中声明

---

## 四、回调地址校验

### 4.1 URL 校验器设计

`AppURLValidator` 继承自 Django 的 `URLValidator`，重写了正则表达式以支持 `localhost`：

```python
class AppURLValidator(URLValidator):
    host_re = "(" + validator.hostname_re + validator.domain_re + "|localhost)"
    regex = re.compile(
        r"^(?:[a-z0-9.+-]*)://"  # scheme
        r"(?:[^\s:@/]+(?::[^\s:@/]*)?@)?"  # user:pass
        r"(?:" + validator.ipv4_re + "|" + validator.ipv6_re + "|" + host_re + ")"
        r"(?::\d{2,5})?"  # port
        r"(?:[/?#][^\s]*)?"  # path
        r"\Z",
        re.IGNORECASE,
    )
```

**校验逻辑:**
- 支持 localhost 作为主机名
- 支持 IPv4、IPv6 地址
- 端口号为 2-5 位数字（可选）
- 协议通过 schemes 参数控制

### 4.2 扩展点 URL 规则

扩展点 URL 支持相对路径（以 `/` 开头），但**必须满足以下条件**：

- Manifest 中必须提供 `appUrl` 作为基础 URL

如果 URL 不以 `/` 开头，则必须是完整的绝对 URL，使用 `AppURLValidator()` 校验。

### 4.3 Token 回调地址 (`tokenTargetUrl`)

**校验规则:**
- 使用 `AppURLValidator()`（无 schemes 参数）
- 允许的协议继承 Django URLValidator 默认值

**运行时行为:**
- 仅在 Manifest 中声明了 `tokenTargetUrl` 时才生成 Token
- Token 发送失败会触发回滚（删除已创建的 App）

---

## 五、Token 颁发机制

### 5.1 Token 生成与存储

**触发条件:**
- Manifest 文件中声明了 `tokenTargetUrl` 字段

**Token 属性:**
- 使用 `oauthlib.common.generate_token()` 生成随机字符串
- 数据库仅存储哈希值（使用 Django 的 `make_password`）
- 明文 Token 仅在创建时返回，之后无法从数据库恢复
- 存储 `token_last_4` 字段用于用户识别

**生成代码:**
```python
# AppTokenManager.create()
if not auth_token:
    auth_token = generate_token()  # 调用 oauthlib 生成随机字符串
app_token.set_auth_token(auth_token)  # 哈希存储
# 返回 (app_token, auth_token) - 明文仅此时可见
```

### 5.2 Token 发送流程

```
Manifest 声明了 tokenTargetUrl
    │
    ▼
App.tokens.create(name="Default token")
    ├─ 生成明文 Token（内存中）
    ├─ 哈希后存储到 DB
    └─ 返回明文 Token
            │
            ▼
send_app_token(target_url, token)
    ├─ Headers:
    │   ├─ Saleor-Domain
    │   ├─ Saleor-Api-Url
    │   └─ Saleor-Schema-Version
    ├─ Body: {"auth_token": "<明文token>"}
    └─ POST 请求，allow_redirects=False
            │
            ├─ 成功（2xx）→ 继续安装
            └─ 失败 → 回滚
                ├─ app.delete() 级联删除所有关联
                └─ 抛出异常，任务标记为 FAILED
```

### 5.3 安全设计要点

1. **禁止重定向**：`allow_redirects=False`
2. **哈希存储**：数据库不存储明文 Token
3. **失败回滚**：Token 发送失败立即删除 App
4. **多 Token 支持**：每个 App 可拥有多个独立 Token

---

## 六、对 Webhook 通道的影响

### 6.1 Webhook 自动创建

安装流程会根据清单中的 `webhooks` 字段自动创建 Webhook 及其关联事件：

```python
# 1. 批量创建 Webhook
webhooks = Webhook.objects.bulk_create([
    Webhook(
        app=app,
        name=webhook["name"],
        is_active=webhook["isActive"],
        target_url=webhook["targetUrl"],
        subscription_query=webhook["query"],
        custom_headers=webhook.get("customHeaders", None),
    )
    for webhook in manifest_data.get("webhooks", [])
])

# 2. 批量创建 WebhookEvent
webhook_events = []
for db_webhook, manifest_webhook in zip(webhooks, manifest_data.get("webhooks", [])):
    for event_type in manifest_webhook["events"]:
        webhook_events.append(
            WebhookEvent(webhook=db_webhook, event_type=event_type)
        )
WebhookEvent.objects.bulk_create(webhook_events)
```

### 6.2 事件类型校验

**异步事件 (`asyncEvents`):**
- 必须是 `WebhookEventAsyncType` 中定义的合法事件

**同步事件 (`syncEvents`):**
- 必须是 `WebhookEventSyncType` 中定义的合法事件

**事件来源优先级:**
1. 如果清单中显式声明了 `asyncEvents` 或 `syncEvents`，使用声明的事件
2. 如果未声明，从 `subscription_query` 中解析事件

### 6.3 权限与 Webhook 联动

Webhook 的事件分发受应用权限控制：

```python
# AppQueryset.for_event_type
def for_event_type(self, event_type: str):
    permissions = {}
    required_permission = WebhookEventAsyncType.PERMISSIONS.get(
        event_type, WebhookEventSyncType.PERMISSIONS.get(event_type)
    )
    if required_permission:
        app_label, codename = required_permission.value.split(".")
        permissions["permissions__content_type__app_label"] = app_label
        permissions["permissions__codename"] = codename
    return self.filter(
        is_active=True,
        webhooks__is_active=True,
        webhooks__events__event_type=event_type,
        **permissions,
    )
```

**分发规则:**
| 条件 | 结果 |
|------|------|
| App.is_active = False | 不发送 |
| Webhook.is_active = False | 不发送 |
| App 无事件所需权限 | 不发送 |
| Webhook 未订阅该事件 | 不发送 |
| 以上全部满足 | 发送 |

### 6.4 Webhook 生命周期管理

**创建:**
- 安装时根据清单批量创建
- 自动关联到所属 App
- 默认为激活状态

**更新:**
- 清单驱动的安装不支持自动更新 Webhook

**删除:**
- 应用删除时级联删除所有关联 Webhook
- 同时清理待发送的 EventDelivery、EventDeliveryAttempt、EventPayload

---

## 七、关键安全控制点

| 控制点 | 位置 | 目的 |
|--------|------|------|
| MANAGE_APPS 权限禁止 | `_ensure_app_permissions_allowed()`<br>`ensure_app_permissions_allowed()` | 防止权限提升 |
| 请求者权限范围校验 | `ensure_can_manage_permissions()` | 防止越权授权 |
| Token 发送失败回滚 | `install_app()` | 避免残留无效凭证 |
| App 标识符唯一性 | `clean_manifest_data()` | 防止重复安装 |
| Webhook 权限过滤 | `AppQueryset.for_event_type()` | 事件按需分发 |
| URL 协议白名单 | `AppURLValidator` | 协议范围控制 |
| 禁止重定向 | `send_app_token()` | 防止 Token 泄露 |
| 哈希存储 | `AppToken.set_auth_token()` | 密码学安全 |

---

## 八、错误处理机制

### 8.1 安装失败场景分类

| 阶段 | 失败场景 | 处理方式 |
|------|---------|---------|
| Mutation 输入校验 | Manifest URL 格式错误 | 直接返回 GraphQL 错误 |
| Mutation 输入校验 | 权限包含 MANAGE_APPS | 直接返回 GraphQL 错误 |
| Mutation 输入校验 | 请求者权限不足 | 直接返回 GraphQL 错误 |
| 异步任务 | Manifest 拉取失败 | 任务标记为 FAILED |
| 异步任务 | Manifest 验证失败 | 任务标记为 FAILED |
| 异步任务 | Token 发送失败 | 回滚 App，任务标记为 FAILED |
| 异步任务 | 数据库操作失败 | 任务标记为 FAILED |

### 8.2 失败处理流程

```
安装失败
    │
    ▼
AppInstallation.status = FAILED
    │
    ├─ 已创建的 App → 删除（如 Token 发送失败）
    ├─ 已创建的 Webhook → 级联删除
    ├─ 已创建的 AppExtension → 级联删除
    ├─ 已创建的 AppToken → 级联删除
    └─ AppInstallation 记录 → 保留
            │
            ▼
记录错误消息到 AppInstallation.message
```

---

## 九、模块协作关系

```
┌─────────────────────────────────────────────────────────────┐
│                 GraphQL Layer                                │
│              saleor/graphql/app/                             │
├─────────────────────────────────────────────────────────────┤
│  mutations/app_install.py        → Mutation 入口             │
│  mutations/app_fetch_manifest.py → 清单预览                  │
│  utils.py                        → 权限校验工具              │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    App Domain                               │
│                  saleor/app/                                │
├─────────────────────────────────────────────────────────────┤
│  models.py                       → App, AppToken,            │
│                                    AppInstallation 模型      │
│  manifest_schema.py              → Manifest Pydantic 定义    │
│  manifest_validations.py         → 清单业务验证              │
│  installation_utils.py           → 安装执行核心逻辑          │
│  tasks.py                        → Celery 异步任务           │
│  validators.py                   → AppURLValidator           │
└─────────────────────────┬───────────────────────────────────┘
                          │
            ┌─────────────┴─────────────┐
            ▼                           ▼
┌──────────────────────┐    ┌──────────────────────────────┐
│   Webhook Domain     │    │      Permission Domain       │
│  saleor/webhook/     │    │     saleor/permission/       │
├──────────────────────┤    ├──────────────────────────────┤
│  models.py           │    │  enums.py                    │
│  event_types.py      │    │  models.py                   │
└──────────────────────┘    └──────────────────────────────┘
```

---

## 十、设计原则总结

第三方应用清单接入流程体现了以下设计原则：

1. **最小权限原则**
   - 禁止授予 `MANAGE_APPS` 权限
   - 请求者不能越权授予权限
   - Webhook 事件分发受权限控制

2. **原子性操作**
   - Token 发送失败时回滚已创建资源

3. **深度防御**
   - 多层验证（结构、业务、权限）
   - 权限黑名单双重检查

4. **异步处理**
   - 网络 IO 操作通过 Celery 异步执行

---

## 附录：关键代码位置速查表

| 功能 | 文件路径 | 关键函数/类 |
|------|---------|------------|
| Manifest 结构定义 | `saleor/app/manifest_schema.py` | `ManifestSchema` |
| Manifest 业务验证 | `saleor/app/manifest_validations.py` | `clean_manifest_data()` |
| 安装执行逻辑 | `saleor/app/installation_utils.py` | `install_app()` |
| 异步安装任务 | `saleor/app/tasks.py` | `install_app_task()` |
| App 安装 Mutation | `saleor/graphql/app/mutations/app_install.py` | `AppInstall` |
| GraphQL 层权限校验 | `saleor/graphql/app/utils.py` | `ensure_app_permissions_allowed()` |
| URL 验证器 | `saleor/app/validators.py` | `AppURLValidator` |
| App 模型 | `saleor/app/models.py` | `App`, `AppToken`, `AppInstallation` |
| Webhook 事件分发过滤 | `saleor/app/models.py` | `AppQueryset.for_event_type()` |
| 权限枚举 | `saleor/permission/enums.py` | `AppPermission`, `get_permission_names()` |

---

**文档说明**：本文档基于 Saleor 源码分析编写。
