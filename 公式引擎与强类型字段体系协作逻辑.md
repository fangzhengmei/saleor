# 公式引擎与强类型字段体系协作逻辑

## 1. 系统概述

Saleor 电商平台中，**公式引擎**（Expression Engine）与**强类型字段体系**（Strongly Typed Field System）是支撑促销规则、价格计算、属性管理等核心业务能力的两大基础子系统。两者通过清晰的职责边界和标准化的协作接口，实现了灵活可扩展的业务规则表达与执行。

---

## 2. 核心子系统定义

### 2.1 强类型字段体系

**位置**：`saleor/attribute/`、`saleor/graphql/attribute/`

#### 核心组件

| 组件 | 职责 | 关键文件 |
|------|------|----------|
| **Attribute 模型** | 定义属性元数据，包括输入类型、实体类型、单位等 | `saleor/attribute/models/base.py:116` |
| **AttributeValue 模型** | 存储属性值，为每种输入类型提供专门的存储字段 | `saleor/attribute/models/base.py:365` |
| **AttributeInputType 枚举** | 定义所有支持的属性输入类型 | `saleor/attribute/__init__.py:1` |
| **AssignedAttribute 接口** | GraphQL 层的强类型属性解析接口 | `saleor/graphql/attribute/types.py:760` |

#### 支持的输入类型

```python
AttributeInputType.DROPDOWN        # 下拉选择
AttributeInputType.MULTISELECT     # 多选
AttributeInputType.FILE            # 文件
AttributeInputType.REFERENCE       # 多引用
AttributeInputType.SINGLE_REFERENCE # 单引用
AttributeInputType.NUMERIC         # 数值
AttributeInputType.RICH_TEXT       # 富文本
AttributeInputType.PLAIN_TEXT      # 纯文本
AttributeInputType.SWATCH          # 色板
AttributeInputType.BOOLEAN         # 布尔
AttributeInputType.DATE            # 日期
AttributeInputType.DATE_TIME       # 日期时间
```

#### 引用类型映射

通过 `ENTITY_TYPE_MAPPING` 实现引用类型与实体模型的绑定：
- `Page` → `reference_page` 字段
- `Product` → `reference_product` 字段
- `ProductVariant` → `reference_variant` 字段
- `Category` → `reference_category` 字段
- `Collection` → `reference_collection` 字段

---

### 2.2 公式引擎（表达式评估系统）

**位置**：`saleor/graphql/discount/utils.py`、`saleor/graphql/core/connection.py`

#### 核心组件

| 组件 | 职责 | 关键文件 |
|------|------|----------|
| **filter_qs_by_predicate** | 谓词表达式主入口，处理 AND/OR 逻辑运算 | `saleor/graphql/discount/utils.py:180` |
| **where_filter_qs** | 复杂 Where 表达式解析器，支持嵌套条件 | `saleor/graphql/core/connection.py:554` |
| **PredicateObjectType** | 谓词作用域枚举（CATALOGUE/CHECKOUT/ORDER） | `saleor/graphql/discount/utils.py:34` |
| **PREDICATE_TO_HANDLE_METHOD** | 谓词字段到处理方法的映射表 | `saleor/graphql/discount/utils.py:160` |

#### 表达式结构

```json
{
  "AND": [
    {"productPredicate": {"category": {"ids": ["..."]}}},
    {
      "OR": [
        {"variantPredicate": {"price": {"gte": 100}}},
        {"collectionPredicate": {"slug": {"eq": "sale"}}}
      ]
    }
  ]
}
```

---

## 3. 协作流程详解

### 3.1 整体数据流

```
强类型字段定义
       ↓
属性值存储（类型安全）
       ↓
表达式解析（公式引擎）
       ↓
字段引用解析（类型匹配）
       ↓
求值执行（数据库查询）
       ↓
结果回写（模型更新）
```

---

### 3.2 表达式解析阶段

**核心函数**：`filter_qs_by_predicate(predicate, base_qs, predicate_type)`

#### 解析流程：

1. **逻辑运算符提取**：从谓词中提取 `AND`、`OR` 运算符
2. **递归处理**：对嵌套的谓词结构进行递归解析
3. **字段分发**：根据谓词字段类型分发到对应的处理方法

```python
# saleor/graphql/discount/utils.py:180-221
def filter_qs_by_predicate(predicate, base_qs, predicate_type, ...):
    and_data = predicate.pop("AND", None)
    or_data = predicate.pop("OR", None)
    
    if and_data:
        result_qs = _handle_and_data(...)
    if or_data:
        result_qs = _handle_or_data(...)
    if predicate:
        result_qs = _handle_predicate(...)
    
    return result_qs
```

---

### 3.3 字段引用解析阶段

**核心机制**：`PREDICATE_TO_HANDLE_METHOD` 映射表

#### 字段处理映射：

| 谓词字段 | 处理方法 | 强类型验证 |
|----------|----------|------------|
| `productPredicate` | `_handle_product_predicate` | `ProductWhere` |
| `variantPredicate` | `_handle_variant_predicate` | `ProductVariantWhere` |
| `collectionPredicate` | `_handle_collection_predicate` | `CollectionWhere` |
| `categoryPredicate` | `_handle_category_predicate` | `CategoryWhere` |

#### 类型安全保障：

每个处理方法内部通过 `where_filter_qs` 调用对应的 `Where` 输入类型进行强类型验证：

```python
# saleor/graphql/discount/utils.py:91-103
def _handle_product_predicate(predicate_data, variant_qs):
    product_qs = where_filter_qs(
        Product.objects.filter(...),
        {},
        ProductWhere,  # 强类型验证
        predicate_data,
        None,
    )
    return ProductVariant.objects.filter(
        Exists(product_qs.filter(id=OuterRef("product_id")))
    )
```

---

### 3.4 求值执行阶段

**核心函数**：`where_filter_qs(iterable, args, filterset_class, filter_input, request)`

#### 执行流程：

1. **运算符检测**：检查是否包含 `AND`/`OR`/`NOT` 运算符
2. **嵌套处理**：递归处理嵌套的表达式结构
3. **过滤执行**：调用 `filter_qs` 执行实际的数据库查询过滤
4. **结果合并**：根据运算符类型（AND/OR）合并查询结果

```python
# saleor/graphql/core/connection.py:554-630
def where_filter_qs(iterable, args, filterset_class, filter_input, ...):
    if contains_filter_operator(filter_input) and len(filter_input) > 1:
        raise GraphQLError("Cannot mix operators with other filter inputs.")
    
    and_filter_input = filter_input.pop("AND", None)
    or_filter_input = filter_input.pop("OR", None)
    
    if and_filter_input:
        queryset = _handle_and_filter_input(...)
    if or_filter_input:
        queryset = _handle_or_filter_input(...)
    if filter_input:
        queryset &= filter_qs(...)
    
    return queryset
```

---

### 3.5 结果回写阶段

#### 折扣结果回写：

**模型**：`OrderDiscount`、`OrderLineDiscount`、`CheckoutDiscount`、`CheckoutLineDiscount`

```python
# saleor/discount/utils/shared.py:21-68
def update_discount(rule, voucher, discount_name, ..., discount_to_update, ...):
    if voucher and discount_to_update.voucher_id != voucher.id:
        discount_to_update.voucher_id = voucher.id
        updated_fields.append("voucher_id")
    # ... 更新其他字段
```

#### 价格结果回写：

直接更新到订单/结账模型的金额字段：

```python
# saleor/order/calculations.py:141-149
order.save(
    update_fields=[
        "subtotal_net_amount",
        "subtotal_gross_amount",
        "total_net_amount",
        "total_gross_amount",
        # ... 其他价格字段
    ]
)
```

---

## 4. 循环依赖检测与处理

### 4.1 循环导入处理

**问题**：模块间的相互引用导致导入时循环依赖

**解决方案**：函数内部延迟导入

```python
# saleor/order/delivery_context.py:80-85
# Circular import caused by the current definition of subscription payloads
# and their usage in webhook/transport layer. Until moving them out from the
# transport, we will have circular imports.
from .webhooks.exclude_shipping import (
    excluded_shipping_methods_for_order,
)
```

### 4.2 计算顺序依赖

**问题**：价格计算、折扣应用、税费计算之间存在依赖关系

**解决方案**：明确的计算流水线

```
基础价格计算
       ↓
目录促销应用（Catalogue Promotion）
       ↓
订单促销应用（Order Promotion）
       ↓
优惠券应用（Voucher）
       ↓
手动折扣应用（Manual Discount）
       ↓
税费计算（Tax Calculation）
```

### 4.3 并发安全

通过数据库行级锁和原子操作避免竞态条件：

```python
# saleor/order/calculations.py:129-131
locked_order = (
    order_qs_select_for_update().only("updated_at").get(id=order.id)
)
```

---

## 5. 子系统分工矩阵

| 功能模块 | 强类型字段体系 | 公式引擎 |
|----------|----------------|----------|
| **表达式语法定义** | ❌ | ✅ |
| **字段类型定义** | ✅ | ❌ |
| **字段值存储** | ✅ | ❌ |
| **逻辑运算符解析** | ❌ | ✅ |
| **字段引用验证** | ✅ | ✅（协作） |
| **类型安全检查** | ✅ | ❌ |
| **查询构建** | ❌ | ✅ |
| **结果回写** | ✅ | ❌ |
| **循环依赖检测** | ❌ | ✅（部分） |

---

## 6. 关键设计模式

### 6.1 策略模式（Strategy Pattern）

通过 `PREDICATE_TO_HANDLE_METHOD` 映射表，为不同类型的谓词字段选择不同的处理策略。

### 6.2 组合模式（Composite Pattern）

表达式结构支持无限嵌套，通过递归处理实现组合逻辑。

### 6.3 类型映射模式（Type Mapping Pattern）

通过 `ENTITY_TYPE_MAPPING` 和 `ASSIGNED_ATTRIBUTE_MAP` 实现类型级别的动态分发。

### 6.4 管道与过滤器模式（Pipeline and Filter Pattern）

价格计算采用流水线处理，每个阶段专注于单一职责。

---

## 7. 扩展点

### 7.1 新增属性输入类型

1. 在 `AttributeInputType` 中添加新类型
2. 在 `AttributeValue` 模型中添加对应存储字段
3. 创建对应的 `AssignedXxxAttribute` GraphQL 类型
4. 在 `ASSIGNED_ATTRIBUTE_MAP` 中注册映射关系

### 7.2 新增谓词类型

1. 创建新的 `XxxWhere` 输入类型
2. 实现对应的处理方法 `_handle_xxx_predicate`
3. 在 `PREDICATE_TO_HANDLE_METHOD` 中注册映射

---

## 8. 总结

公式引擎与强类型字段体系通过**职责分离**、**接口标准化**和**类型安全保障**三大设计原则，构建了一个既灵活又可靠的业务规则执行平台。强类型体系提供了坚实的数据基础，公式引擎提供了强大的表达能力，两者的协作使得 Saleor 能够高效处理复杂的促销、定价和属性管理场景。
